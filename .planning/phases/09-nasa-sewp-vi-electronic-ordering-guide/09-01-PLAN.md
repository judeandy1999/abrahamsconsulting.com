---
phase: 09-nasa-sewp-vi-electronic-ordering-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/nasa-sewp-vi.ts
  - src/content/schema.ts
  - scripts/validate-content.mjs
  - lib/content/nasa-sewp-vi-page.ts
  - components/marketing/NasaSewpViElectronicOrderingGuideSection.tsx
  - components/marketing/NasaSewpViOrderingGuideCard.tsx
  - components/marketing/NasaSewpViBody.tsx
  - app/globals.css
  - public/images/nasa-sewp-vi/ordering-guide-document.webp
autonomous: true
requirements:
  - EOG-01
  - EOG-02
  - EOG-03
  - EOG-04
  - EOG-05
  - EOG-06
  - EOG-07
  - EOG-08
must_haves:
  truths:
    - "User scrolling past Contract Overview on /nasa-sewp-vi immediately sees the Electronic Ordering Guide section (D-01)"
    - "Section displays title, intro copy, and four feature bullets from typed content (D-04, D-08, EOG-04)"
    - "Desktop shows feature list left and elevated download card right; mobile stacks with download card first (D-02, D-03, EOG-02, EOG-03)"
    - "Download card extends NasaSewpViOrderingGuideCard with illustration slot and no metadata rows (D-07, D-09, EOG-05)"
    - "Download card is elevated white card with rounded corners and soft shadow matching federal contact card (D-14)"
    - "Feature list uses circular light-blue Lucide icon backgrounds per consulting capabilities pattern (D-15)"
    - "Section title Electronic Ordering Guide with red accent line under heading, no eyebrow (D-16)"
    - "Download card shows card title, supporting text, and Download PDF button only — no Last Updated or Version rows (D-09, EOG-05 aligned)"
    - "When ordering-guide.pdf exists, Download PDF opens NASA_SEWP_VI_DOCUMENTS.orderingGuide in a new tab (D-11, D-12, EOG-07)"
    - "When ordering-guide.pdf is absent, section still renders with disabled Coming Soon control — no broken href (D-10, EOG-06)"
    - "No second ordering-guide download link elsewhere on the page; orderingProcess removed (D-05, D-06)"
    - "npm run content:check, typecheck, lint, and build all pass (EOG-08)"
  artifacts:
    - path: src/content/nasa-sewp-vi.ts
      provides: electronicOrderingGuide content block with mockup copy; orderingProcess removed; resources.orderingGuide removed
      contains: electronicOrderingGuide
    - path: src/content/schema.ts
      provides: Zod schema for electronicOrderingGuide; orderingProcess and resources.orderingGuide removed
    - path: lib/content/nasa-sewp-vi-page.ts
      provides: Runtime isAvailable injection on electronicOrderingGuide.download from filesystem check
      exports: loadNasaSewpViPageContent
    - path: components/marketing/NasaSewpViElectronicOrderingGuideSection.tsx
      provides: Two-column section with motion, feature list, and download card slot
      min_lines: 60
    - path: components/marketing/NasaSewpViOrderingGuideCard.tsx
      provides: Elevated download card with WebP illustration and available/coming-soon states
    - path: components/marketing/NasaSewpViBody.tsx
      provides: Section wired immediately after NasaSewpViContractOverviewSection
    - path: public/images/nasa-sewp-vi/ordering-guide-document.webp
      provides: Custom PDF document illustration per D-13
    - path: app/globals.css
      provides: sewp-vi-eordering-* section layout and extended sewp-vi-ordering-guide__* card styles
  key_links:
    - from: components/marketing/NasaSewpViBody.tsx
      to: components/marketing/NasaSewpViElectronicOrderingGuideSection.tsx
      via: render immediately after NasaSewpViContractOverviewSection
      pattern: NasaSewpViElectronicOrderingGuideSection
    - from: components/marketing/NasaSewpViElectronicOrderingGuideSection.tsx
      to: content.electronicOrderingGuide
      via: section prop from NasaSewpViBody content
      pattern: electronicOrderingGuide
    - from: components/marketing/NasaSewpViOrderingGuideCard.tsx
      to: NASA_SEWP_VI_DOCUMENTS.orderingGuide
      via: download.href + isAvailable from loadNasaSewpViPageContent
      pattern: target="_blank".*ordering-guide|isAvailable
    - from: lib/content/nasa-sewp-vi-page.ts
      to: public/documents/nasa-sewp-vi/ordering-guide.pdf
      via: existsSync on NASA_SEWP_VI_DOCUMENTS.orderingGuide path
      pattern: existsSync.*orderingGuide
---

<objective>
Deliver the mockup-accurate Electronic Ordering Guide section on `/nasa-sewp-vi` — a standalone white block directly below Contract Overview with typed content, four feature bullets, an elevated download card with custom illustration, and PDF availability wired to the existing document path pattern.

Purpose: Satisfy NASA SEWP CHUM requirement that the ordering guide be discoverable on the website before first order; consolidate scattered ordering-guide scaffolding into one authoritative section.
Output: Content module + schema, section component, extended download card, CSS, WebP asset, body integration, legacy cleanup.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-nasa-sewp-vi-electronic-ordering-guide/09-CONTEXT.md
@src/content/nasa-sewp-vi.ts
@src/content/schema.ts
@lib/content/nasa-sewp-vi-page.ts
@components/marketing/NasaSewpViBody.tsx
@components/marketing/NasaSewpViContractOverviewSection.tsx
@components/marketing/NasaSewpViOrderingGuideCard.tsx
@components/marketing/ConsultingServicesPackageSection.tsx
@app/globals.css

## Multi-Source Coverage Audit

| Source | Item | Plan Task |
|--------|------|-----------|
| GOAL | Section after Contract Overview with PDF download | Task 2 (wire), Task 3 (verify) |
| GOAL | Mockup-accurate layout | Task 2 + Task 3 |
| EOG-01 | Placement after Contract Overview | Task 2 |
| EOG-02 | Two-column desktop layout | Task 2 + Task 3 |
| EOG-03 | Mobile stack without overflow | Task 3 |
| EOG-04 | Title, intro, four features from content module | Task 1 + Task 2 |
| EOG-05 | Download card heading, text, action (no metadata per D-09) | Task 1 + Task 2 |
| EOG-06 | Coming soon when PDF missing | Task 1 + Task 2 |
| EOG-07 | Configurable href via NASA_SEWP_VI_DOCUMENTS.orderingGuide | Task 1 |
| EOG-08 | Schema validation, a11y, typography/motion patterns | Task 1 + Task 2 + Task 3 |
| D-01–D-16 | All locked decisions | Tasks 1–3 (see task actions) |
| RESEARCH | N/A (no RESEARCH.md for phase 9) | — |
| Deferred | None | — |

**EOG-05 note:** REQUIREMENTS.md mentions Last Updated/Version; D-09 removes them. Implement card with title, description, and Download PDF / Coming Soon button only.

<interfaces>
From lib/content/nasa-sewp-vi-page.ts (current — refactor in Task 1):
```typescript
export function loadNasaSewpViPageContent(): NasaSewpViPageContent {
  // Currently injects isAvailable on resources.orderingGuide — move to electronicOrderingGuide.download
}
```

From src/content/nasa-sewp-vi.ts:
```typescript
export const NASA_SEWP_VI_DOCUMENTS = {
  capabilityStatement: "/documents/nasa-sewp-vi/capability-statement.pdf",
  orderingGuide: "/documents/nasa-sewp-vi/ordering-guide.pdf"
} as const;
```

From components/marketing/NasaSewpViOrderingGuideCard.tsx (extend, do not replace):
```typescript
type OrderingGuide = NonNullable<NasaSewpViPageContent["resources"]["orderingGuide"]>;
// After Task 1: prop type becomes electronicOrderingGuide.download shape
```

From components/marketing/NasaSewpViBody.tsx (insertion point):
```typescript
<NasaSewpViContractOverviewSection section={content.contractOverview} />
// NEW section goes here — before AboutSewp
```

Mockup copy (from 09-CONTEXT.md):
- Intro: "Our Electronic Ordering Guide provides federal agencies with step-by-step instructions for placing orders through the NASA SEWP VI contract with Abrahams Consulting LLC."
- Features: Easy to Download, Always Up to Date, Print-Ready Format, Required Before First Order (exact descriptions in CONTEXT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content schema, electronicOrderingGuide block, and loader availability</name>
  <read_first>src/content/schema.ts (nasaSewpViPageSchema), src/content/nasa-sewp-vi.ts, scripts/validate-content.mjs (nasaSewpViPageSchema mirror), lib/content/nasa-sewp-vi-page.ts, 09-CONTEXT.md decisions D-05 through D-12 and D-08</read_first>
  <files>src/content/nasa-sewp-vi.ts, src/content/schema.ts, scripts/validate-content.mjs, lib/content/nasa-sewp-vi-page.ts</files>
  <action>
    Add `NASA_SEWP_VI_EORDERING_ASSETS` constant with `documentIllustrationSrc: "/images/nasa-sewp-vi/ordering-guide-document.webp"` and alt text for the PDF graphic (D-13).

    Replace `orderingProcess` with `electronicOrderingGuide` in content and both Zod schemas (schema.ts + validate-content.mjs mirror). Schema shape per D-08:
    - `title` (string) — "Electronic Ordering Guide"
    - `intro` (string) — mockup intro from CONTEXT
    - `features` — array of exactly 4 items: `{ id, icon, title, description }` where `icon` is a new enum (e.g. `download`, `refresh`, `printer`, `shield-check` per D-16 discretion)
    - `download` — `{ title, description, downloadLabel, comingSoonLabel, href, illustrationSrc, illustrationAlt, isAvailable?: boolean }` where `href` references `NASA_SEWP_VI_DOCUMENTS.orderingGuide` (D-12, EOG-07). Do NOT add lastUpdated or version fields (D-09).

    Remove `orderingProcess` object entirely from nasa-sewp-vi.ts and both schemas (D-05).

    Remove `resources.orderingGuide` from content and schemas; keep `resources.capabilityStatement` for hero CTA (D-06 minimal removal).

    Update `loadNasaSewpViPageContent()` to inject `download.isAvailable` via `existsSync` on `public` + `NASA_SEWP_VI_DOCUMENTS.orderingGuide` — same pattern as today but targeting `electronicOrderingGuide.download` instead of `resources.orderingGuide` (D-10, D-12).

    Populate content with exact mockup feature titles/descriptions from 09-CONTEXT.md `<specifics>`. Card copy: heading like "SEWP VI Electronic Ordering Guide", supporting sentence about downloading the official PDF, `downloadLabel: "Download PDF"`, `comingSoonLabel: "Coming Soon"`.
  </action>
  <acceptance_criteria>
    - `orderingProcess` absent from content module and both validators
    - `electronicOrderingGuide` present with 4 features and download block referencing NASA_SEWP_VI_DOCUMENTS.orderingGuide
    - No lastUpdated/version fields in schema or content
    - `resources.orderingGuide` removed; capabilityStatement unchanged
    - Loader sets download.isAvailable from filesystem without requiring code changes when PDF is added
  </acceptance_criteria>
  <verify>
    <automated>npm run content:check</automated>
  </verify>
  <done>Content schema and loader deliver electronicOrderingGuide with runtime PDF availability; legacy orderingProcess and resources.orderingGuide removed.</done>
</task>

<task type="auto">
  <name>Task 2: Section component, extended download card, and body wiring</name>
  <read_first>components/marketing/NasaSewpViContractOverviewSection.tsx, components/marketing/NasaSewpViOrderingGuideCard.tsx, components/marketing/NasaSewpViBody.tsx, components/marketing/ConsultingServicesPackageSection.tsx (icon circle pattern), components/marketing/NasaSewpViFederalSalesContactSection.tsx (elevated card reference), components/marketing/pillarIconProps.ts, components/marketing/marketing-motion.ts</read_first>
  <files>components/marketing/NasaSewpViElectronicOrderingGuideSection.tsx, components/marketing/NasaSewpViOrderingGuideCard.tsx, components/marketing/NasaSewpViBody.tsx</files>
  <action>
    Create `NasaSewpViElectronicOrderingGuideSection` (client component) per D-01, D-02, D-03, D-04, D-16:
    - White full-width section (`sewp-vi-eordering` BEM root), `aria-labelledby` pointing at h2
    - Title "Electronic Ordering Guide" with red accent line under heading (match `.sewp-vi-overview__title` accent pattern — no eyebrow, D-16)
    - Intro paragraph from content
    - Two-column grid: left = feature list; right = download card
    - Feature rows: circular light-blue icon background + Lucide icon via `pillarIconProps` (match consulting-services-package / federal contact patterns, D-15); row dividers between features
    - `useMarketingMotionConfig()` for container/item variants on section entrance (D-04)
    - Mobile: use CSS grid `order` so download card column renders first, feature list second (D-03)

    Extend `NasaSewpViOrderingGuideCard` per D-07, D-09, D-10, D-11, D-14:
    - Accept `download` prop typed from `NasaSewpViPageContent["electronicOrderingGuide"]["download"]`
    - Render elevated white card: Next.js `Image` for WebP illustration (D-13), card title, description, primary button
    - When `isAvailable`: render `<a>` (not `<button>`) with `href={download.href}`, `target="_blank"`, `rel="noopener noreferrer"`, class `btn btn--primary` — remove `download` attribute so PDF opens in new tab (D-11). Accessible name includes download label.
    - When not available: render disabled-looking control with `comingSoonLabel`, `aria-disabled="true"`, no href — reuse coming-soon visual language from existing stub (D-10, EOG-06)
    - Do NOT render Last Updated or Version rows anywhere (D-09, EOG-05)

    Wire into `NasaSewpViBody`: import section; render `<NasaSewpViElectronicOrderingGuideSection section={content.electronicOrderingGuide} />` immediately after `NasaSewpViContractOverviewSection` and before `NasaSewpViAboutSewpSection` (D-01, EOG-01). Confirm no other component on the page renders an ordering-guide download link (D-06).
  </action>
  <acceptance_criteria>
    - New section file exports and renders title, intro, 4 features, and download card from typed content
    - Body places section directly after Contract Overview
    - Card shows title + description + Download PDF or Coming Soon only — zero metadata rows
    - Available state uses new-tab link; unavailable state has no navigable href
    - Section uses framer-motion scroll-in pattern consistent with adjacent NASA sections
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Electronic Ordering Guide section and extended download card wired into page body immediately after Contract Overview.</done>
</task>

<task type="auto">
  <name>Task 3: Styles, WebP asset, legacy CSS cleanup, and quality gates</name>
  <read_first>app/globals.css (sewp-vi-overview__*, sewp-vi-ordering-guide__*, sewp-vi-federal-contact__card, consulting-services-package__icon, existing @media breakpoints for sewp-vi sections), approved mockup at path in 09-CONTEXT.md canonical_refs</read_first>
  <files>app/globals.css, public/images/nasa-sewp-vi/ordering-guide-document.webp</files>
  <action>
    Add `public/images/nasa-sewp-vi/ordering-guide-document.webp` — export/create from approved mockup PDF illustration graphic (D-13). If source PNG exists in assets, convert to WebP; otherwise create a representative document illustration matching mockup proportions.

    Add `.sewp-vi-eordering` section styles in globals.css (D-02, D-03, EOG-02, EOG-03, EOG-08):
    - White background, container max-width, vertical padding aligned with adjacent NASA sections
    - Desktop: two-column grid with gap; left column feature list, right column card
    - Mobile/tablet (@media matching existing sewp-vi breakpoints ~768px): single column, `order: -1` on card column so card appears above features (D-03); no horizontal overflow
    - Title typography and red accent underline consistent with Contract Overview heading treatment (D-16)
    - Feature row layout: icon circle (light blue bg), title + description, bottom borders between rows (D-15)

    Extend/refactor `.sewp-vi-ordering-guide__*` classes for elevated card layout (D-14): rounded corners, soft shadow matching `.sewp-vi-federal-contact__card`, centered illustration area, full-width primary button on narrow viewports.

    Remove unused `.sewp-vi-ordering__steps`, `.sewp-vi-ordering__step*`, `.sewp-vi-ordering__actions` rules if no component references them after orderingProcess removal (dead CSS cleanup).

    Run full quality pipeline and fix any lint/type/build issues introduced by schema or component changes.
  </action>
  <acceptance_criteria>
    - WebP illustration exists at path referenced by NASA_SEWP_VI_EORDERING_ASSETS
    - Desktop two-column and mobile card-first layouts render without horizontal scroll
    - Download card matches elevated federal-contact card visual weight
    - Dead orderingProcess CSS removed where unreferenced
    - content:check, typecheck, lint, and production build all pass
  </acceptance_criteria>
  <verify>
    <automated>npm run content:check; npm run typecheck; npm run lint; npm run build</automated>
  </verify>
  <done>Styles, WebP asset, and quality gates complete; section matches mockup layout on desktop and mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content module → static page | Typed content defines PDF href; build-time validation only |
| Browser → public PDF | User follows external link to static file in /public/documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | download.href in content | mitigate | href sourced only from NASA_SEWP_VI_DOCUMENTS constant; zod validates string path at build time |
| T-09-02 | Spoofing | PDF file on disk | accept | Static hosting; file replacement is intentional ops workflow per EOG-07 |
| T-09-03 | Denial of Service | existsSync at build/load | accept | Single file check; negligible cost for static generation |
| T-09-04 | Information Disclosure | Coming Soon vs available state | mitigate | Do not render href when isAvailable is false; aria-disabled on disabled control |
| T-09-05 | Elevation | Client component | accept | No server secrets; PDF path is public marketing asset |
| T-09-SC | Tampering | public/images asset | mitigate | WebP committed to repo; no npm install in this phase |
</threat_model>

<verification>
After all tasks:
1. Visit `/nasa-sewp-vi` — Contract Overview followed immediately by Electronic Ordering Guide (no About SEWP in between).
2. Confirm four feature bullets with icons and mockup copy.
3. With ordering-guide.pdf absent (current repo state): Coming Soon disabled control, no 404 link.
4. After dropping PDF at `public/documents/nasa-sewp-vi/ordering-guide.pdf`: rebuild shows active Download PDF opening in new tab.
5. Resize to mobile width: download card appears above feature list.
6. Grep page components: no remaining `resources.orderingGuide` or `orderingProcess` references.
</verification>

<success_criteria>
- All EOG-01 through EOG-08 requirements satisfied with D-09 metadata omission on download card
- Single plan executes in one wave without checkpoints
- Production build succeeds with content schema validation
- Section visually matches approved mockup structure on desktop and mobile
</success_criteria>

<output>
Create `.planning/phases/09-nasa-sewp-vi-electronic-ordering-guide/09-01-SUMMARY.md` when done
</output>
