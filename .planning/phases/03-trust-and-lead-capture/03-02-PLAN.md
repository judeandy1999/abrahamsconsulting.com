---
phase: 03-trust-and-lead-capture
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(marketing)/layout.tsx
  - app/(marketing)/trust/page.tsx
  - app/sitemap.ts
  - tests/plan03-trust-route-journey.test.mjs
autonomous: true
requirements:
  - TRST-01
  - TRST-02
  - TRST-03
must_haves:
  truths:
    - "Users can open a dedicated trust route with certifications and qualification signals."
    - "Users can review at least two publishable case snapshots with outcomes-oriented summaries."
    - "Users can view approved partner indicators without degrading static-first rendering behavior."
  artifacts:
    - path: "app/(marketing)/trust/page.tsx"
      provides: "Dedicated trust page with certifications, case snapshots, and partner indicators"
    - path: "app/(marketing)/layout.tsx"
      provides: "Primary navigation discoverability for trust content"
    - path: "tests/plan03-trust-route-journey.test.mjs"
      provides: "Automated trust-route and discoverability contracts"
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "app/(marketing)/trust/page.tsx"
      via: "primary navigation link"
      pattern: "href=\"/trust\""
    - from: "app/(marketing)/trust/page.tsx"
      to: "lib/content/load-content.ts"
      via: "typed trust content loader consumption"
      pattern: "loadMarketingContent"
    - from: "app/sitemap.ts"
      to: "/trust"
      via: "crawl discoverability route entry"
      pattern: "/trust"
---

<objective>
Implement the trust content journey as a dedicated, discoverable marketing route built from validated Phase 3 trust contracts.

Purpose: Deliver `D-02` and `D-03` plus `TRST-01` through `TRST-03` with static-first rendering and deterministic trust-route regression checks.
Output: New `/trust` page, navigation/sitemap wiring, and automated trust journey tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-trust-and-lead-capture/03-trust-and-lead-capture-CONTEXT.md
@.planning/phases/03-trust-and-lead-capture/03-01-PLAN.md
@app/(marketing)/layout.tsx
@app/sitemap.ts

<interfaces>
From `lib/content/load-content.ts` after Plan `03-01`:
- `loadMarketingContent()` returns trust dataset in addition to existing site/service/contract data.

From `app/(marketing)/layout.tsx`:
- Navigation links are static `Link` entries and should include `/trust` in the same server-rendered pattern.

From `app/sitemap.ts`:
- `marketingRoutes` array drives crawl output and must include trust route for discoverability.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define trust-route journey contract tests</name>
  <files>tests/plan03-trust-route-journey.test.mjs</files>
  <behavior>
    - Navigation exposes `/trust` for primary discoverability.
    - Trust route renders certifications and case snapshot sections with partner indicators.
    - Sitemap includes `/trust` path.
  </behavior>
  <action>Create deterministic source-level tests for `TRST-01` to `TRST-03` before route implementation. Assert section markers and route links, and avoid brittle full-copy matching.</action>
  <verify>
    <automated>node --test tests/plan03-trust-route-journey.test.mjs</automated>
  </verify>
  <done>Tests fail initially and identify missing trust route/discoverability contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build dedicated trust route from typed content</name>
  <files>app/(marketing)/trust/page.tsx</files>
  <behavior>
    - `/trust` is static-first and server-rendered per existing marketing route pattern.
    - Trust page exposes certifications and at least two outcomes-focused case snapshots per `D-02`.
    - Partner indicators render with performance-safe image metadata/attributes per `D-03`.
  </behavior>
  <action>Create `app/(marketing)/trust/page.tsx` that consumes the typed trust dataset from Plan `03-01` and renders certifications, case snapshots, and partner indicators. Keep rendering static-first (`force-static`) and avoid client-side data fetching.</action>
  <verify>
    <automated>node --test tests/plan03-trust-route-journey.test.mjs</automated>
  </verify>
  <done>Trust route tests pass and route content satisfies credibility signal requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Wire trust discoverability and validate build safety</name>
  <files>app/(marketing)/layout.tsx, app/sitemap.ts, tests/plan03-trust-route-journey.test.mjs</files>
  <action>Update primary navigation and sitemap route list to expose `/trust`, then run integrated checks. Preserve server-rendered navigation and static route generation patterns from Phases 1-2.</action>
  <verify>
    <automated>node --test tests/plan03-trust-route-journey.test.mjs && npm run lint && npm run build</automated>
  </verify>
  <done>Trust route is discoverable from navigation and sitemap with all quality gates passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trust content data->public page rendering | Credibility claims become publicly visible marketing assertions. |
| public user traffic->trust route | Untrusted traffic hits trust route and linked static assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | trust route content sections | mitigate | Bind trust-section presence and case snapshot minimum behavior to automated tests. |
| T-03-05 | Denial of Service | trust indicator media rendering | mitigate | Use static-first rendering and performance-safe asset usage (no blocking runtime fetch). |
| T-03-06 | Spoofing | trust-route discoverability links | mitigate | Maintain canonical `/trust` navigation/sitemap wiring and regression checks. |
| T-03-SC | Tampering | dependency supply chain | mitigate | No new packages required; if executor introduces one, enforce package legitimacy checkpoint before install. |
</threat_model>

<verification>
Run:
- `node --test tests/plan03-trust-route-journey.test.mjs`
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Dedicated `/trust` route exists and surfaces certifications, case snapshots, and approved partner indicators.
- Trust content is discoverable from both primary navigation and sitemap.
- Automated tests fail on trust-route contract regressions and quality gates remain green.
</success_criteria>

<output>
Create `.planning/phases/03-trust-and-lead-capture/03-02-SUMMARY.md` when done
</output>
