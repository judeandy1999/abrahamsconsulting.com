---
phase: 03-trust-and-lead-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/schema.ts
  - src/content/site.ts
  - src/content/trust.ts
  - lib/content/load-content.ts
  - scripts/validate-content.mjs
  - tests/plan03-trust-lead-content-contract.test.mjs
autonomous: true
requirements:
  - TRST-01
  - TRST-02
  - TRST-03
  - LEAD-02
must_haves:
  truths:
    - "Trust content includes certifications and qualification signals users can review."
    - "Trust content includes at least two outcomes-focused case snapshots."
    - "Lead form field requirements are explicit and validated through typed content contracts."
  artifacts:
    - path: "src/content/trust.ts"
      provides: "Code-managed trust dataset for certifications, case snapshots, and partner indicators"
    - path: "src/content/schema.ts"
      provides: "Typed schemas for trust content and consultation form qualification fields"
    - path: "tests/plan03-trust-lead-content-contract.test.mjs"
      provides: "Automated regression checks for trust and lead content contracts"
  key_links:
    - from: "src/content/schema.ts"
      to: "src/content/trust.ts"
      via: "zod parsing for trust artifacts and case snapshot minimum count"
      pattern: "z\\.array\\(|z\\.object\\("
    - from: "lib/content/load-content.ts"
      to: "src/content/trust.ts"
      via: "centralized marketing content loader wiring"
      pattern: "trust"
---

<objective>
Define Phase 3 trust and lead-capture interface contracts so downstream route and API implementation can execute deterministically.

Purpose: Implement `D-01`, `D-03`, and the qualification-contract portion of `D-04` by extending the existing typed content pipeline before page/endpoint work.
Output: New trust content module, expanded schemas/loader wiring, validator parity updates, and contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/03-trust-and-lead-capture/03-trust-and-lead-capture-CONTEXT.md
@src/content/schema.ts
@lib/content/load-content.ts
@scripts/validate-content.mjs

<interfaces>
From `src/content/schema.ts`:
- `export const marketingContentSchema`
- `export type MarketingContent`
- `site.consultationCta.path` literal contract remains `/consultation`

From `lib/content/load-content.ts`:
- `export function loadMarketingContent(): MarketingContent`
- Loader currently composes `site`, `services`, and `contracts` and must be extended for trust content.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define trust and lead content contract tests</name>
  <files>tests/plan03-trust-lead-content-contract.test.mjs</files>
  <behavior>
    - Trust schema requires certifications plus at least two case snapshots per `D-02`.
    - Partner indicators include performance-safe asset metadata per `D-03`.
    - Lead form field definitions include qualification intent fields per `D-04`.
  </behavior>
  <action>Create deterministic source-level contract tests for trust-content and lead-field schema expectations before implementation. Keep assertions targeted at contract markers and minimum-count guarantees, not brittle copy strings.</action>
  <verify>
    <automated>node --test tests/plan03-trust-lead-content-contract.test.mjs</automated>
  </verify>
  <done>Tests fail initially and clearly identify missing trust/lead contract structures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend typed content and loader contracts</name>
  <files>src/content/schema.ts, src/content/site.ts, src/content/trust.ts, lib/content/load-content.ts</files>
  <behavior>
    - Trust content data is code-managed and typed (`D-01`) with case snapshot minimum enforcement (`D-02`).
    - Partner indicator records include fields needed for optimized/static rendering (`D-03`).
    - Consultation qualification field metadata is represented in typed content for downstream form rendering (`D-04`).
  </behavior>
  <action>Implement trust and lead-related schema/content changes per `D-01` through `D-04`: add trust content types, create `src/content/trust.ts`, extend `site` content for form-field metadata, and wire trust data into `loadMarketingContent`. Keep content modules as static typed data only; do not add executable logic.</action>
  <verify>
    <automated>node --test tests/plan03-trust-lead-content-contract.test.mjs</automated>
  </verify>
  <done>Contract tests pass and loader returns trust plus qualification contract data alongside existing datasets.</done>
</task>

<task type="auto">
  <name>Task 3: Align validator parity and quality gates</name>
  <files>scripts/validate-content.mjs, tests/plan03-trust-lead-content-contract.test.mjs</files>
  <action>Update standalone content validation script to mirror all new trust and lead qualification schema fields so runtime and build-time validation stay in parity. Keep deterministic CLI pass/fail behavior and run repository quality checks after updates.</action>
  <verify>
    <automated>node scripts/validate-content.mjs && node --test tests/plan03-trust-lead-content-contract.test.mjs && npm run lint</automated>
  </verify>
  <done>Validator enforces new contracts and automated checks pass without weakening existing quality gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code-managed trust content->public trust pages | Authored credibility claims cross into user-visible marketing content. |
| content schema->lead form rendering | Qualification-field definitions control what users are asked to submit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/content/trust.ts` claims | mitigate | Enforce strict `zod` validation and source-level tests requiring required trust fields and case count. |
| T-03-02 | Information Disclosure | trust indicator assets | mitigate | Restrict trust indicator records to approved public metadata only; no secrets in content modules. |
| T-03-03 | Repudiation | requirement-to-contract traceability | mitigate | Keep deterministic tests and validator outputs for trust/lead contracts in CI evidence. |
| T-03-SC | Tampering | package/dependency supply chain | mitigate | Implement with existing dependencies only; if any install becomes necessary, gate with package legitimacy checkpoint first. |
</threat_model>

<verification>
Run:
- `node --test tests/plan03-trust-lead-content-contract.test.mjs`
- `node scripts/validate-content.mjs`
- `npm run lint`
</verification>

<success_criteria>
- Typed trust content contracts exist and enforce certifications, case snapshots, and partner indicator metadata.
- Consultation qualification fields are formally represented in validated content contracts.
- Validator and tests detect schema drift between content modules and runtime loader expectations.
</success_criteria>

<output>
Create `.planning/phases/03-trust-and-lead-capture/03-01-SUMMARY.md` when done
</output>
