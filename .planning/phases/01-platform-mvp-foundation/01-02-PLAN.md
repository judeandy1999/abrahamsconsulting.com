---
phase: 01-platform-mvp-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/(marketing)/layout.tsx
  - app/(marketing)/page.tsx
  - app/(marketing)/services/page.tsx
  - app/(marketing)/contracts/page.tsx
  - app/sitemap.ts
  - app/robots.ts
  - lib/seo/metadata.ts
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - "Users get fast first-load behavior from statically rendered core marketing routes."
    - "Users can navigate between core pages without request-time rendering bottlenecks."
  artifacts:
    - path: "app/(marketing)/services/page.tsx"
      provides: "Core marketing route rendered static-first"
    - path: "app/(marketing)/contracts/page.tsx"
      provides: "Procurement-oriented core route rendered static-first"
    - path: "lib/seo/metadata.ts"
      provides: "Reusable metadata and canonical helpers for marketing pages"
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "app/(marketing)/page.tsx"
      via: "shared shell and navigation links"
      pattern: "href="
    - from: "app/(marketing)/page.tsx"
      to: "lib/seo/metadata.ts"
      via: "metadata generation"
      pattern: "generateMetadata|metadata"
---

<objective>
Implement static-first rendering behavior and route-level SEO primitives for core marketing pages so users experience fast navigation.

Purpose: Deliver `PLAT-02` per `D-02` with real static-first routes and shared shell wiring, not placeholders.
Output: Core marketing route set using static defaults, reusable metadata helper, and crawl primitives (`sitemap`/`robots`) tied to route structure.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-platform-mvp-foundation/01-platform-mvp-foundation-CONTEXT.md
@.planning/phases/01-platform-mvp-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define static-first behavior for core marketing routes</name>
  <files>app/(marketing)/page.tsx, app/(marketing)/services/page.tsx, app/(marketing)/contracts/page.tsx</files>
  <behavior>
    - Route modules expose static-friendly behavior (`dynamic = "force-static"` or equivalent no-request-time data path).
    - Core pages render successfully in production build without runtime fetch dependencies.
  </behavior>
  <action>Implement core marketing routes with static-first rendering semantics per `D-02` and ensure page content is sourced from deterministic in-repo data (no runtime API dependency). Avoid dynamic server logic in these routes unless explicitly required by requirement scope.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Core routes build as static-first pages and are included in the generated route manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared marketing shell navigation wiring</name>
  <files>app/(marketing)/layout.tsx, app/(marketing)/page.tsx</files>
  <action>Create shared marketing layout navigation linking core routes so user navigation stays deterministic and fast per `D-02`. Keep interaction minimal and server-first; avoid unnecessary client hydration in shell-level components.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Marketing layout provides working links among Home, Services, and Contracts routes with build success.</done>
</task>

<task type="auto">
  <name>Task 3: Add metadata and crawl primitives tied to route model</name>
  <files>lib/seo/metadata.ts, app/sitemap.ts, app/robots.ts</files>
  <action>Implement reusable metadata helper and generated crawl primitives aligned to current route map, supporting static-first discoverability and preventing early SEO drift from route additions.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Metadata helper and crawl files compile, and generated output includes core marketing routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user->public routes | Untrusted web traffic reaches static marketing pages. |
| app routes->search crawler interpretation | Metadata and crawl directives influence indexing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | route metadata | mitigate | Keep metadata helpers deterministic and avoid exposing secret env-derived values in metadata output. |
| T-01-05 | Tampering | route rendering mode | mitigate | Enforce static-first settings for core routes and verify through build output each change. |
| T-01-06 | Denial of Service | dynamic rendering on core pages | mitigate | Avoid runtime data dependencies on primary routes; keep request-time work out of marketing pages. |
| T-01-SC | Tampering | package changes during implementation | mitigate | Do not add unvetted dependencies; keep implementation within approved baseline stack. |
</threat_model>

<verification>
Run:
- `npm run build`
- `npm run lint`
- Validate generated `sitemap`/`robots` include core routes.
</verification>

<success_criteria>
- Core marketing routes are static-first and build without request-time data dependencies.
- Shared navigation works across core pages.
- Metadata/crawl primitives are generated from route model and remain consistent.
</success_criteria>

<output>
Create `.planning/phases/01-platform-mvp-foundation/01-02-SUMMARY.md` when done
</output>
