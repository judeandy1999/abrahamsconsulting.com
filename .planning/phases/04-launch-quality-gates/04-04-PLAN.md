---
phase: 04-launch-quality-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
files_modified:
  - app/globals.css
  - app/(marketing)/layout.tsx
  - app/(marketing)/consultation/page.tsx
  - app/(marketing)/consultation/success/page.tsx
  - eslint.config.mjs
  - lighthouserc.json
  - tests/plan04-a11y-gates.test.mjs
autonomous: true
requirements:
  - A11Y-01
must_haves:
  truths:
    - "Keyboard users can skip repetitive navigation and reach main content on marketing pages."
    - "Focus states are visible on links, buttons, and form controls across primary routes."
    - "Consultation form controls have programmatic labels and the submit path is keyboard-operable."
  artifacts:
    - path: "app/(marketing)/layout.tsx"
      provides: "Skip link and landmark structure for marketing shell"
    - path: "app/globals.css"
      provides: "Focus-visible styles meeting WCAG 2.2 AA contrast intent"
    - path: "tests/plan04-a11y-gates.test.mjs"
      provides: "Automated a11y contract checks for shell and consultation form"
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "app/(marketing)/consultation/page.tsx"
      via: "shared shell wrapping consultation route"
      pattern: "skip|main id="
    - from: "app/(marketing)/consultation/page.tsx"
      to: "/api/lead"
      via: "native form submit"
      pattern: "action=\"/api/lead\""
---

<objective>
Harden keyboard and assistive-technology support on primary marketing routes and the consultation form to WCAG 2.2 AA-aligned launch behavior.

Purpose: Deliver `A11Y-01` per `D-05` with shell skip-link/landmarks, visible focus, labeled form controls, and CI accessibility budget enforcement without client-heavy refactors per `D-06`.
Output: Marketing shell accessibility upgrades, consultation form semantics, eslint jsx-a11y guardrails, extended Lighthouse accessibility assertions, and contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-launch-quality-gates/04-launch-quality-gates-CONTEXT.md
@app/(marketing)/layout.tsx
@app/(marketing)/consultation/page.tsx
@app/globals.css
@eslint.config.mjs
@lighthouserc.json

<interfaces>
Consultation form today:
- Native POST to `/api/lead` with labeled fields via `htmlFor`/`id`
- Honeypot field uses `aria-hidden="true"` and off-screen positioning

Marketing shell today:
- `nav` has `aria-label` but no skip link and no `main` landmark wrapper at layout level
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define accessibility gate contract tests</name>
  <files>tests/plan04-a11y-gates.test.mjs</files>
  <behavior>
    - Marketing layout includes skip link targeting main content landmark.
    - Global CSS defines `:focus-visible` styles for links, buttons, and form controls.
    - Consultation page keeps label associations for every rendered field and exposes submit button type.
    - Lighthouse config includes accessibility category minimum score assertion.
  </behavior>
  <action>Create source-level a11y contract tests per `D-05` before UI changes. Prefer stable attribute/markup assertions over full axe runs in this task file; Lighthouse accessibility category enforcement is configured in plan 04-03 `lighthouserc.json` and extended here if not yet present.</action>
  <verify>
    <automated>node --test tests/plan04-a11y-gates.test.mjs</automated>
  </verify>
  <done>A11y contract tests fail until skip link, focus styles, and consultation semantics are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade marketing shell landmarks and focus visibility</name>
  <files>app/(marketing)/layout.tsx, app/globals.css, eslint.config.mjs</files>
  <action>Per `D-05`, add a visually hidden skip link (`href="#main-content"`) as the first focusable element in `app/(marketing)/layout.tsx`. Do not nest a second `main` in the layout because route pages already render `main`; instead ensure every marketing route `main` element uses `id="main-content"` (update route files in this task only where missing). Add `:focus-visible` outlines with sufficient contrast in `app/globals.css` for `a`, `button`, `input`, `select`, and `textarea`. Enable `eslint-plugin-jsx-a11y` recommended rules in `eslint.config.mjs` for `app/**` to catch regressions (anchor-is-valid, label-has-associated-control where applicable). Do not add client-side navigation libraries.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Lint passes with jsx-a11y rules enabled and layout includes skip link + main landmark markers.</done>
</task>

<task type="auto">
  <name>Task 3: Harden consultation form and success page semantics</name>
  <files>app/(marketing)/consultation/page.tsx, app/(marketing)/consultation/success/page.tsx, lighthouserc.json, tests/plan04-a11y-gates.test.mjs</files>
  <action>Ensure consultation form uses `fieldset`/`legend` or equivalent grouping only if it improves screen-reader context without harming layout; keep native POST flow to `/api/lead`. Add `aria-describedby` for required field hint text where appropriate. On success page, provide clear heading hierarchy and link back to `/consultation` and `/` with visible focus states. Extend `lighthouserc.json` accessibility assertions (if not already present from plan 04-03) to require accessibility score >= 0.95 on `/consultation`. Preserve honeypot accessibility isolation. Run a11y contract tests.</action>
  <verify>
    <automated>node --test tests/plan04-a11y-gates.test.mjs && npm run build</automated>
  </verify>
  <done>A11y contract tests pass and consultation routes build with improved semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user->marketing UI | Keyboard and AT users interact with public form fields and navigation. |
| honeypot field | Must remain hidden from AT while not trapping keyboard focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Elevation of Privilege | consultation form | mitigate | Keep server-side validation in `/api/lead`; UI changes must not bypass honeypot or required field checks. |
| T-04-08 | Information Disclosure | skip link / labels | accept | Accessibility labels expose only public form field names already visible to users. |
| T-04-SC | Tampering | eslint-plugin-jsx-a11y install | mitigate | Use official eslint-plugin-jsx-a11y package from npm; verify name before install. |
</threat_model>

<verification>
Run:
- `node --test tests/plan04-a11y-gates.test.mjs`
- `npm run lint`
- `npm run build`
- Keyboard manual check: Tab from Home through nav to Consultation, submit form focus order remains logical.
</verification>

<success_criteria>
- Skip link and main landmark exist on all marketing pages.
- Focus-visible styles apply to interactive controls.
- Consultation form and success route pass a11y contract tests and Lighthouse accessibility budget config.
</success_criteria>

<output>
Create `.planning/phases/04-launch-quality-gates/04-04-SUMMARY.md` when done
</output>
