---
phase: 04-launch-quality-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - package.json
  - package-lock.json
  - app/layout.tsx
  - app/globals.css
  - app/(marketing)/trust/page.tsx
  - lighthouserc.json
  - .github/workflows/ci.yml
  - tests/plan04-performance-gates.test.mjs
autonomous: true
requirements:
  - PERF-01
user_setup:
  - service: vercel-speed-insights
    why: "Production Core Web Vitals field data by route"
    env_vars: []
    dashboard_config:
      - task: "Enable Speed Insights for the Vercel project after deploy"
        location: "Vercel Dashboard -> Project -> Speed Insights"
must_haves:
  truths:
    - "Production deploys emit real-user CWV telemetry for launch routes via Speed Insights."
    - "CI fails when Lighthouse performance budgets for primary launch pages regress beyond STACK thresholds."
  artifacts:
    - path: "lighthouserc.json"
      provides: "Lighthouse CI budgets mapped to CWV-oriented categories"
    - path: ".github/workflows/ci.yml"
      provides: "CI job running Lighthouse against production build"
    - path: "tests/plan04-performance-gates.test.mjs"
      provides: "Source-level checks for Speed Insights wiring and budget config presence"
  key_links:
    - from: "app/layout.tsx"
      to: "@vercel/speed-insights"
      via: "SpeedInsights component in root layout"
      pattern: "SpeedInsights"
    - from: ".github/workflows/ci.yml"
      to: "lighthouserc.json"
      via: "lhci autorun using budget file"
      pattern: "lhci|lighthouserc"
---

<objective>
Add production RUM and CI Lighthouse performance gates so launch pages meet agreed Core Web Vitals thresholds.

Purpose: Deliver `PERF-01` per `D-04` using `@vercel/speed-insights` for field data and Lighthouse CI budgets on primary routes, while preserving static-first marketing rendering per `D-06`.
Output: Speed Insights integration, Lighthouse CI config and workflow step, performance contract tests, and trust-page media optimizations that protect LCP/CLS.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/phases/04-launch-quality-gates/04-launch-quality-gates-CONTEXT.md
@app/layout.tsx
@app/(marketing)/trust/page.tsx
@.github/workflows/ci.yml

<interfaces>
CWV targets (p75) from STACK:
- LCP <= 2.5s
- INP <= 200ms
- CLS <= 0.1

CI today: lint, typecheck, build only.
Trust page already uses `next/image` for partner indicators — verify width/height/priority attributes during optimization pass.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define performance gate contract tests</name>
  <files>tests/plan04-performance-gates.test.mjs</files>
  <behavior>
    - Root layout imports and renders Speed Insights component.
    - `lighthouserc.json` exists with assertions for performance score and CWV-oriented audits (LCP, CLS, TBT/INP proxy).
    - CI workflow includes a Lighthouse step after production build.
  </behavior>
  <action>Create source-level contract tests per `D-04` that fail before Speed Insights and Lighthouse CI wiring exist. Encode STACK threshold intent in comments and budget config keys, not duplicated magic numbers inside tests.</action>
  <verify>
    <automated>node --test tests/plan04-performance-gates.test.mjs</automated>
  </verify>
  <done>Performance contract tests fail until RUM and Lighthouse CI artifacts are present.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Speed Insights and protect LCP/CLS on trust media</name>
  <files>package.json, package-lock.json, app/layout.tsx, app/globals.css, app/(marketing)/trust/page.tsx</files>
  <action>Install `@vercel/speed-insights` (official Vercel package) and render `<SpeedInsights />` in `app/layout.tsx` per `D-04`. Avoid adding analytics scripts beyond Speed Insights in this plan. Audit `app/(marketing)/trust/page.tsx` partner logos: ensure `next/image` uses explicit dimensions, meaningful `alt`, and avoids layout-shifting styles; add minimal global CSS only if needed for stable image containers (no heavy frameworks). Do not convert marketing pages to client components for metrics.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build succeeds with Speed Insights dependency installed and trust media remains static-friendly.</done>
</task>

<task type="auto">
  <name>Task 3: Add Lighthouse CI budgets to GitHub Actions</name>
  <files>lighthouserc.json, .github/workflows/ci.yml, package.json, package-lock.json</files>
  <action>Add `@lhci/cli` as a devDependency and `lighthouserc.json` budgets aligned to `D-04` thresholds on routes: `/`, `/services`, `/contracts`, `/trust`, `/consultation`. Extend `.github/workflows/ci.yml` to build, start the production server, run `lhci autorun` against localhost, and fail on budget violations. Keep job runtime reasonable (single run per URL or modest collection). Do not add autoplay media, large JS bundles, or third-party martech scripts.</action>
  <verify>
    <automated>node --test tests/plan04-performance-gates.test.mjs</automated>
  </verify>
  <done>Performance contract tests pass and CI workflow contains Lighthouse gate wiring (local full LHCI run may be validated in plan summary, not required on every dev machine).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->Vercel analytics | RUM beacons leave user browsers to Vercel infrastructure. |
| CI->localhost server | Lighthouse probes built site over loopback during CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | Speed Insights | accept | Collects performance metrics only; no form field values or lead PII. |
| T-04-06 | Denial of Service | Lighthouse CI | mitigate | Limit probed URLs to primary launch routes; run after build in CI only. |
| T-04-SC | Tampering | npm installs (`@vercel/speed-insights`, `@lhci/cli`) | mitigate | Use official Vercel package and established Lighthouse CI CLI; verify package names on npm before install. |
</threat_model>

<verification>
Run:
- `node --test tests/plan04-performance-gates.test.mjs`
- `npm run build`
- `npm run lint`
- `npm run typecheck`
</verification>

<success_criteria>
- Speed Insights is wired in the root layout for production RUM.
- Lighthouse CI config and workflow step enforce CWV-oriented budgets on primary launch routes.
- PERF-01 performance contract tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-launch-quality-gates/04-03-SUMMARY.md` when done
</output>
