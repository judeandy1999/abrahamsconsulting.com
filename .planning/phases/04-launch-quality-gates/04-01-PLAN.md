---
phase: 04-launch-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/schema.ts
  - src/content/seo.ts
  - lib/seo/metadata.ts
  - lib/seo/page-seo.ts
  - app/(marketing)/page.tsx
  - app/(marketing)/about/page.tsx
  - app/(marketing)/services/page.tsx
  - app/(marketing)/services/[slug]/page.tsx
  - app/(marketing)/contracts/page.tsx
  - app/(marketing)/trust/page.tsx
  - app/(marketing)/consultation/page.tsx
  - app/(marketing)/consultation/success/page.tsx
  - tests/plan04-page-metadata-contract.test.mjs
autonomous: true
requirements:
  - SEO-01
must_haves:
  truths:
    - "Every launch marketing route exposes intent-aligned title, description, and canonical metadata."
    - "Service detail pages derive metadata from typed service content rather than hardcoded duplicates."
  artifacts:
    - path: "src/content/seo.ts"
      provides: "Validated per-route SEO definitions for launch pages"
    - path: "lib/seo/page-seo.ts"
      provides: "Lookup helpers mapping route keys and service slugs to metadata options"
    - path: "tests/plan04-page-metadata-contract.test.mjs"
      provides: "Automated regression for metadata coverage on all launch routes"
  key_links:
    - from: "app/(marketing)/services/[slug]/page.tsx"
      to: "lib/seo/page-seo.ts"
      via: "generateMetadata using service slug"
      pattern: "generateMetadata|buildMarketingMetadata"
    - from: "lib/seo/page-seo.ts"
      to: "src/content/seo.ts"
      via: "typed SEO registry lookup"
      pattern: "launchPageSeo|pageSeo"
---

<objective>
Establish a typed launch SEO registry and wire complete per-route metadata across all marketing pages.

Purpose: Deliver `SEO-01` per `D-01` by closing metadata gaps on `/about`, `/services`, `/contracts`, `/services/[slug]`, and `/consultation/success` while keeping canonical generation centralized.
Output: Validated SEO content module, metadata lookup helpers, updated route metadata exports, and contract tests that fail when a launch route lacks SEO coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/04-launch-quality-gates/04-launch-quality-gates-CONTEXT.md

<interfaces>
From `lib/seo/metadata.ts`:
- `export function getSiteUrl(): string`
- `export function buildMarketingMetadata(options: MarketingMetadataOptions): Metadata`

From `src/content/schema.ts`:
- `export const serviceItemSchema` (includes `slug`, `title`, `summary`)
- `export function loadMarketingContent()` via `lib/content/load-content.ts`

Launch routes requiring metadata after Phase 3:
- `/`, `/about`, `/services`, `/services/[slug]`, `/contracts`, `/trust`, `/consultation`, `/consultation/success`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define launch page metadata contract tests</name>
  <files>tests/plan04-page-metadata-contract.test.mjs</files>
  <behavior>
    - SEO registry includes static launch paths: `/`, `/about`, `/services`, `/contracts`, `/trust`, `/consultation`, `/consultation/success`.
    - Each registry entry requires non-empty `title` and `description` strings.
    - Contract test asserts every static launch route module imports metadata via `buildMarketingMetadata` or `generateMetadata`.
    - Service detail route module must reference dynamic metadata generation tied to service slug.
  </behavior>
  <action>Create source-level contract tests that encode the launch metadata matrix per `D-01`. Assert against route file sources and the SEO registry module rather than brittle marketing copy strings. Tests must fail before registry implementation exists.</action>
  <verify>
    <automated>node --test tests/plan04-page-metadata-contract.test.mjs</automated>
  </verify>
  <done>Metadata contract tests fail with clear missing-route or missing-registry signals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed launch SEO registry and lookup helpers</name>
  <files>src/content/schema.ts, src/content/seo.ts, lib/seo/page-seo.ts, lib/seo/metadata.ts</files>
  <behavior>
    - `launchPageSeoSchema` validates route key, path, title, and description for static pages.
    - `getStaticPageSeo(path)` returns metadata options for static launch routes.
    - `getServicePageSeo(slug)` resolves service detail metadata from typed services content with not-found-safe behavior delegated to route layer.
  </behavior>
  <action>Extend zod schemas and add `src/content/seo.ts` with intent-aligned titles/descriptions for each static launch route per `D-01`. Implement `lib/seo/page-seo.ts` lookup helpers that return `MarketingMetadataOptions` consumed by `buildMarketingMetadata`. Keep canonical path construction in `lib/seo/metadata.ts` unchanged. Do not duplicate service business copy outside typed content modules.</action>
  <verify>
    <automated>npm run content:check</automated>
  </verify>
  <done>SEO registry validates via content check and lookup helpers compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire metadata exports on all launch marketing routes</name>
  <files>app/(marketing)/page.tsx, app/(marketing)/about/page.tsx, app/(marketing)/services/page.tsx, app/(marketing)/services/[slug]/page.tsx, app/(marketing)/contracts/page.tsx, app/(marketing)/trust/page.tsx, app/(marketing)/consultation/page.tsx, app/(marketing)/consultation/success/page.tsx</files>
  <action>Replace ad-hoc or missing metadata on launch routes with registry-backed `metadata` exports per `D-01`. For `app/(marketing)/services/[slug]/page.tsx`, add `generateMetadata` that uses `getServicePageSeo(slug)` and preserves `force-static` behavior per `D-06`. Ensure `/consultation/success` uses noindex-style robots metadata only if required for UX; otherwise keep indexable metadata aligned to confirmation intent. Avoid duplicating title/description literals inside route files.</action>
  <verify>
    <automated>node --test tests/plan04-page-metadata-contract.test.mjs && npm run build</automated>
  </verify>
  <done>All launch routes build successfully and metadata contract tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public route metadata | Crawlers and social platforms consume generated title/description/canonical values. |
| content registry | Typed SEO definitions influence indexable page signals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | per-route metadata literals | mitigate | Centralize SEO definitions in validated `src/content/seo.ts` and enforce via contract tests. |
| T-04-02 | Information Disclosure | metadata helper | mitigate | Keep metadata generation free of secrets; only use `NEXT_PUBLIC_SITE_URL` and public marketing copy. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new runtime dependencies in this plan. |
</threat_model>

<verification>
Run:
- `node --test tests/plan04-page-metadata-contract.test.mjs`
- `npm run content:check`
- `npm run build`
- `npm run lint`
- `npm run typecheck`
</verification>

<success_criteria>
- All launch marketing routes export registry-backed metadata with canonical URLs.
- Service detail metadata is generated from typed service slugs.
- SEO-01 contract tests pass in CI.
</success_criteria>

<output>
Create `.planning/phases/04-launch-quality-gates/04-01-SUMMARY.md` when done
</output>
