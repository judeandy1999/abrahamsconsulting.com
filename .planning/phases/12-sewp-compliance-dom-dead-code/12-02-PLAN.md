---
phase: 12-sewp-compliance-dom-dead-code
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/marketing/NasaSewpViPastPerformanceSection.tsx
  - components/marketing/NasaSewpViExperienceIcon.tsx
  - components/marketing/NasaSewpViOrderingGuideCard.tsx
  - lib/content/nasa-sewp-vi-page.ts
  - src/content/nasa-sewp-vi.ts
  - src/content/schema.ts
  - app/styles/pages/sewp-vi.css
autonomous: true
requirements:
  - SEWP-03
  - SEWP-04
must_haves:
  truths:
    - "Unused SEWP past performance content, schema, and UI components are gone from the SEWP page pipeline"
    - "Unused VPAT / coming-soon / isAvailable Ordering Guide paths are gone"
    - "Ordering Guide Download PDF CTA still links to /documents/nasa-sewp-vi/ordering-guide.pdf when that file is present"
    - "npm run content:check and production build succeed after schema/content cleanup"
    - "Capabilities-statement pastPerformance schemas/content remain untouched"
  artifacts:
    - path: components/marketing/NasaSewpViPastPerformanceSection.tsx
      provides: Deleted orphan section (must not exist)
    - path: components/marketing/NasaSewpViExperienceIcon.tsx
      provides: Deleted orphan icon helper (must not exist)
    - path: components/marketing/NasaSewpViOrderingGuideCard.tsx
      provides: Download PDF CTA only — no accessibility/VPAT/isAvailable vars
      contains: download.href
    - path: lib/content/nasa-sewp-vi-page.ts
      provides: loadNasaSewpViPageContent without VPAT/isAvailable filesystem mutation
      exports: ["loadNasaSewpViPageContent"]
    - path: src/content/nasa-sewp-vi.ts
      provides: electronicOrderingGuide without accessibility/VPAT/comingSoon; no pastPerformance block; no orderingGuideVpat path
    - path: src/content/schema.ts
      provides: nasaSewpViPageSchema without pastPerformance and without EOG accessibility/comingSoon/isAvailable
  key_links:
    - from: components/marketing/NasaSewpViOrderingGuideCard.tsx
      to: electronicOrderingGuide.download.href
      via: always-rendered Download PDF anchor
      pattern: href=\{download\.href\}
    - from: src/content/nasa-sewp-vi.ts
      to: NASA_SEWP_VI_DOCUMENTS.orderingGuide
      via: download.href kept
      pattern: orderingGuide:
    - from: lib/content/nasa-sewp-vi-page.ts
      to: nasaSewpViPageContent
      via: return typed content without mutating availability flags
      pattern: loadNasaSewpViPageContent
---

<objective>
Remove unused SEWP past-performance and Ordering Guide VPAT/coming-soon/isAvailable dead code while keeping the working Ordering Guide PDF download CTA.

Purpose: Satisfy SEWP-03/SEWP-04 cleanup without rewiring Coming Soon or touching capabilities-statement pastPerformance.
Output: Deleted orphan components, slimmed schema/content/loader/card/CSS, automated content:check + HTML assertions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sewp-compliance-dom-dead-code/12-RESEARCH.md
@components/marketing/NasaSewpViOrderingGuideCard.tsx
@components/marketing/NasaSewpViPastPerformanceSection.tsx
@components/marketing/NasaSewpViExperienceIcon.tsx
@lib/content/nasa-sewp-vi-page.ts
@src/content/nasa-sewp-vi.ts
@src/content/schema.ts
@app/styles/pages/sewp-vi.css
@components/marketing/NasaSewpViBody.tsx

## Multi-Source Coverage Audit (this plan)

| Source | Item | Status |
|--------|------|--------|
| GOAL | Unused past-performance and VPAT UI paths gone; OG download still works | COVERED — Tasks 1–3 |
| REQ | SEWP-03 remove past performance from SEWP pipeline | COVERED — Task 1 |
| REQ | SEWP-04 remove unused VPAT/coming-soon; keep OG PDF download | COVERED — Task 2–3 |
| RESEARCH | Delete PastPerformance + ExperienceIcon + schema/content/CSS | COVERED — Task 1 |
| RESEARCH | Strip VPAT/isAvailable/comingSoon; keep download CTA | COVERED — Task 2 |
| RESEARCH | Do not delete capabilities-statement pastPerformance | COVERED — Task 1 constraint |
| RESEARCH | Do not reintroduce Coming Soon UI | COVERED — Task 2 |
| CONTEXT | No CONTEXT.md (Locked Decisions in RESEARCH) | COVERED via RESEARCH |

<interfaces>
From NasaSewpViOrderingGuideCard.tsx (today):
- Destructures `download` and unused `accessibility`; computes unused `orderingGuideAvailable` / `vpatAvailable`
- Always renders `<a href={download.href}>` Download PDF — keep this CTA markup

From lib/content/nasa-sewp-vi-page.ts (today):
- `documentIsAvailable` via existsSync for orderingGuide + orderingGuideVpat
- Mutates `download.isAvailable` and `accessibility.vpat.isAvailable`

From schema.ts nasaSewpViPageSchema:
- `electronicOrderingGuide.download.comingSoonLabel` + optional `isAvailable`
- entire `electronicOrderingGuide.accessibility` object (VPAT)
- `pastPerformance` object using `nasaSewpViPastPerformanceSchema` (L456–461, L739–744)
- Do NOT touch capabilities-statement `pastPerformance` schemas near L989 / L1066

From nasa-sewp-vi.ts:
- `NASA_SEWP_VI_DOCUMENTS.orderingGuideVpat` — delete with VPAT paths
- Keep `orderingGuide` document path and `electronicOrderingGuide.download` title/description/href/illustration/downloadLabel
- Delete `pastPerformance` block (~L546–587)
- Delete `comingSoonLabel` and entire `accessibility` block under electronicOrderingGuide

After cleanup, `loadNasaSewpViPageContent` may simply return `nasaSewpViPageContent` (no filesystem probes) because availability flags are removed per SEWP-04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete SEWP past performance orphans (SEWP-03)</name>
  <files>components/marketing/NasaSewpViPastPerformanceSection.tsx, components/marketing/NasaSewpViExperienceIcon.tsx, src/content/nasa-sewp-vi.ts, src/content/schema.ts, app/styles/pages/sewp-vi.css</files>
  <action>
  Remove unused past performance from the SEWP page pipeline only (SEWP-03; RESEARCH Pattern 3):

  1. Delete `components/marketing/NasaSewpViPastPerformanceSection.tsx` (never imported by `NasaSewpViBody`).
  2. Delete `components/marketing/NasaSewpViExperienceIcon.tsx` (only consumed by PastPerformanceSection).
  3. In `src/content/nasa-sewp-vi.ts`, delete the entire `pastPerformance: { ... }` object from `nasaSewpViPageContent`.
  4. In `src/content/schema.ts`, delete `nasaSewpViPastPerformanceSchema` and the `pastPerformance` field on `nasaSewpViPageSchema` only. Do NOT remove or alter capabilities-statement `pastPerformance` schemas/fields (used by `/capabilities-*` pages).
  5. In `app/styles/pages/sewp-vi.css`, delete unused `.sewp-vi-experience*` rules (~L1093–1221 per RESEARCH). Do not delete unrelated SEWP styles.
  6. Confirm `NasaSewpViBody.tsx` still does not reference past performance (no wire-up required — already absent).
  7. Grep the repo for `NasaSewpViPastPerformanceSection`, `NasaSewpViExperienceIcon`, and `nasaSewpViPastPerformanceSchema` — expect zero matches after deletion.
  </action>
  <verify>
    <automated>if (Test-Path components/marketing/NasaSewpViPastPerformanceSection.tsx) { throw "PastPerformanceSection still exists" }; if (Test-Path components/marketing/NasaSewpViExperienceIcon.tsx) { throw "ExperienceIcon still exists" }; $hits = rg -n "NasaSewpViPastPerformanceSection|NasaSewpViExperienceIcon|nasaSewpViPastPerformanceSchema" --glob "!**/.planning/**" .; if ($hits) { throw "orphan refs remain: $hits" }; rg -n "pastPerformance" src/content/schema.ts | Select-String -Pattern "nasaSewpViPageSchema|capabilities" -Context 2,0; node -e "const fs=require('fs'); const c=fs.readFileSync('src/content/nasa-sewp-vi.ts','utf8'); if(/\bpastPerformance\s*:/.test(c)) process.exit(1); console.log('sewp-03-delete-ok');"</automated>
  </verify>
  <done>SEWP past performance component, icon, content block, page-schema field, and experience CSS are removed; capabilities-statement pastPerformance remains.</done>
</task>

<task type="auto">
  <name>Task 2: Strip VPAT / coming-soon / isAvailable; keep Ordering Guide PDF download (SEWP-04)</name>
  <files>components/marketing/NasaSewpViOrderingGuideCard.tsx, lib/content/nasa-sewp-vi-page.ts, src/content/nasa-sewp-vi.ts, src/content/schema.ts, app/styles/pages/sewp-vi.css</files>
  <action>
  Remove unused Ordering Guide VPAT/coming-soon/availability paths while keeping the working Download PDF CTA (SEWP-04; RESEARCH Pattern 4). Do not reintroduce Coming Soon UI.

  1. `NasaSewpViOrderingGuideCard.tsx`: Stop destructuring `accessibility`. Delete unused `orderingGuideAvailable` and `vpatAvailable`. Keep illustration, title, description, and the `<a href={download.href}>` Download PDF button (target=_blank, rel, aria-label helpers unchanged).
  2. `src/content/nasa-sewp-vi.ts`:
     - Remove `orderingGuideVpat` from `NASA_SEWP_VI_DOCUMENTS` (keep `capabilityStatement` and `orderingGuide`).
     - On `electronicOrderingGuide.download`, remove `comingSoonLabel` (keep title, description, downloadLabel, href, illustrationSrc, illustrationAlt).
     - Delete the entire `accessibility` / VPAT block under `electronicOrderingGuide`.
  3. `src/content/schema.ts` under `electronicOrderingGuide`: remove `comingSoonLabel`, `isAvailable`, and the entire `accessibility` object. Keep download title/description/downloadLabel/href/illustration fields required.
  4. `lib/content/nasa-sewp-vi-page.ts`: Remove VPAT/orderingGuide `documentIsAvailable` probes and mutations of `isAvailable` / `accessibility.vpat`. Simplify `loadNasaSewpViPageContent` to return `nasaSewpViPageContent` (or equivalent spread with no availability fields). Remove unused `existsSync` / `join` imports if no longer needed.
  5. `app/styles/pages/sewp-vi.css`: Delete dead Ordering Guide rules for `__cta--disabled`, `__a11y*`, `__vpat*` (~L2898–2972 per RESEARCH). Keep styles used by the live download card.
  6. Do not delete `public/documents/nasa-sewp-vi/ordering-guide.pdf`. Do not add VPAT Coming Soon UI.
  </action>
  <verify>
    <automated>npm run content:check; npm run typecheck; node -e "const fs=require('fs'); const card=fs.readFileSync('components/marketing/NasaSewpViOrderingGuideCard.tsx','utf8'); if(/accessibility|vpatAvailable|orderingGuideAvailable|isAvailable|comingSoon/.test(card)) process.exit(1); if(!/download\.href/.test(card)) process.exit(2); const loader=fs.readFileSync('lib/content/nasa-sewp-vi-page.ts','utf8'); if(/orderingGuideVpat|isAvailable|accessibility/.test(loader)) process.exit(3); const content=fs.readFileSync('src/content/nasa-sewp-vi.ts','utf8'); if(/orderingGuideVpat|comingSoonLabel/.test(content)) process.exit(4); if(!/orderingGuide:\s*\"\/documents\/nasa-sewp-vi\/ordering-guide\.pdf\"/.test(content)) process.exit(5); const schema=fs.readFileSync('src/content/schema.ts','utf8'); const eog=schema.slice(schema.indexOf('electronicOrderingGuide:'), schema.indexOf('gwacIdentificationStatement:')); if(/comingSoonLabel|isAvailable|accessibility:/.test(eog)) process.exit(6); console.log('sewp-04-strip-ok');"</automated>
  </verify>
  <done>VPAT/coming-soon/isAvailable paths removed from card, loader, content, and schema; Ordering Guide download.href and CTA remain; content:check and typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 3: Build and assert Ordering Guide link present; VPAT / past-performance absent in HTML</name>
  <files>lib/content/nasa-sewp-vi-page.ts, components/marketing/NasaSewpViOrderingGuideCard.tsx</files>
  <action>
  End-to-end verify SEWP-03/SEWP-04 in production HTML (no JS interaction required):

  1. Run `npm run build` (prebuild runs content:check).
  2. Start `npm run start` on port 3000 (or reuse a server serving this build).
  3. `curl.exe -sL http://localhost:3000/nasa-sewp-vi` and assert:
     - PRESENT: `/documents/nasa-sewp-vi/ordering-guide.pdf` (Ordering Guide download kept)
     - PRESENT: `Download PDF` (or downloadLabel text from content)
     - ABSENT: `ordering-guide-vpat`, `VPAT`, `Representative Experience` (dead paths / past performance title must not appear on SEWP page)
  4. Optionally confirm `public/documents/nasa-sewp-vi/ordering-guide.pdf` still exists on disk.
  5. Stop the temporary server if this task started it.
  </action>
  <verify>
    <automated>npm run build; $p = Start-Process -FilePath npm -ArgumentList "run","start" -PassThru -WindowStyle Hidden; Start-Sleep -Seconds 8; try { $html = curl.exe -sL http://localhost:3000/nasa-sewp-vi; if ($html -notlike "*/documents/nasa-sewp-vi/ordering-guide.pdf*") { throw "MISSING ordering-guide.pdf href" }; if ($html -notlike "*Download PDF*") { throw "MISSING Download PDF" }; foreach ($bad in @("ordering-guide-vpat","Representative Experience")) { if ($html -like "*$bad*") { throw "UNEXPECTED: $bad" } }; if ($html -match '(?i)\bVPAT\b') { throw "UNEXPECTED: VPAT" }; if (-not (Test-Path "public/documents/nasa-sewp-vi/ordering-guide.pdf")) { throw "PDF missing on disk" }; Write-Output "sewp-03-04-html-ok" } finally { Stop-Process -Id $p.Id -Force -ErrorAction SilentlyContinue }</automated>
  </verify>
  <done>Production HTML still exposes Ordering Guide PDF download; VPAT and Representative Experience strings are absent; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public `/nasa-sewp-vi` HTML + PDF href | Clients download public marketing documents |
| Build-time Zod content:check | Prevents invalid content schema from shipping |

## STRIDE Threat Register

| Threat ID | Category | Disposition | Mitigation Plan |
|-----------|----------|-------------|-----------------|
| T-12-03 | Spoofing / Tampering perception | mitigate | Keep verified `ordering-guide.pdf` href only; do not invent VPAT download links |
| T-12-04 | Information Disclosure | accept | Deleting orphan past-performance copy reduces surface; no secrets involved |
| T-12-05 | Denial of Service | accept | Removing unused existsSync probes slightly simplifies loader; public static PDF unchanged |
| T-12-SC | Tampering (supply chain) | accept | No new npm packages in this plan |
</threat_model>

<verification>
- Orphan past-performance files deleted; schema/content cleaned
- VPAT/coming-soon/isAvailable stripped; Download PDF remains
- content:check, typecheck, build pass
- curl HTML asserts OG PDF present and VPAT / Representative Experience absent
</verification>

<success_criteria>
- SEWP-03: Past performance absent from SEWP pipeline
- SEWP-04: Unused VPAT/coming-soon paths removed; Ordering Guide PDF download still works
</success_criteria>

<output>
Create `.planning/phases/12-sewp-compliance-dom-dead-code/12-02-SUMMARY.md` when done
</output>
