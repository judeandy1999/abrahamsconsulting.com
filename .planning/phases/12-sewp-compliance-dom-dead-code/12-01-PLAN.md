---
phase: 12-sewp-compliance-dom-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/marketing/NasaSewpViStatementsTableSection.tsx
autonomous: true
requirements:
  - SEWP-01
  - SEWP-02
must_haves:
  truths:
    - "A no-JS fetch of /nasa-sewp-vi HTML contains Fair Opportunity clause text (including FAR Part 16.505) without any tab click"
    - "The same HTML contains Program Manager Maybelline Magnet name, SEWP VI PROGRAM MANAGER role, direct phone (301) 638-8731, and sewp.pm@abrahamsconsulting.com"
    - "All eight statement tabs still switch visual focus; inactive panels use the HTML hidden attribute (not conditional null mount)"
    - "Each tab button aria-controls matches a stable per-tab panel id `${baseId}-panel-${tab.id}`"
  artifacts:
    - path: components/marketing/NasaSewpViStatementsTableSection.tsx
      provides: Always-mounted role=tabpanel per TABS entry with hidden={!isActive}
      contains: "hidden={!isActive}"
  key_links:
    - from: components/marketing/NasaSewpViStatementsTableSection.tsx
      to: fairOpportunity / programManager content props
      via: render all panels in SSR HTML including inactive
      pattern: hidden=\{!isActive\}
    - from: TABS map tab buttons
      to: matching tabpanel ids
      via: aria-controls={`${baseId}-panel-${tab.id}`}
      pattern: aria-controls=\{`\$\{baseId\}-panel-\$\{tab\.id\}`\}
---

<objective>
Refactor NASA SEWP VI statement tabs so every panel (including Fair Opportunity and Program Manager) is present in the initial HTML while preserving existing tab UX via HTML `hidden` on inactive panels.

Purpose: Satisfy SEWP-01/SEWP-02 PMO fetch/skim audits that fail today because only `activeTab === "contract-overview"` mounts.
Output: Updated `NasaSewpViStatementsTableSection` (and CSS only if needed) plus automated HTML presence verification.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sewp-compliance-dom-dead-code/12-RESEARCH.md
@components/marketing/NasaSewpViStatementsTableSection.tsx
@app/styles/pages/sewp-vi.css
@src/content/nasa-sewp-vi.ts

## Multi-Source Coverage Audit (this plan)

| Source | Item | Status |
|--------|------|--------|
| GOAL | Fair Opportunity + Program Manager survive fetch/skim without tab clicks | COVERED — Tasks 1–2 |
| REQ | SEWP-01 Fair Opportunity in initial HTML | COVERED — Tasks 1–2 |
| REQ | SEWP-02 Program Manager Maybelline in initial HTML | COVERED — Tasks 1–2 |
| RESEARCH | Always-mount tabpanels with HTML `hidden` | COVERED — Task 1 |
| RESEARCH | Do not touch Company Hub exclusive-mount | COVERED — out of scope, no task |
| RESEARCH | Leave nested PostDeliveryTopics exclusive-mount | COVERED — out of scope, no task |
| CONTEXT | No CONTEXT.md (decisions in RESEARCH Locked Decisions) | COVERED via RESEARCH |

<interfaces>
<!-- Extracted contracts — executor should use these directly -->

From NasaSewpViStatementsTableSection.tsx:
- `"use client"` component; `useState<TabId>("contract-overview")` must remain the sole initial active tab
- `TABS` has 8 entries; tab buttons already use `aria-controls={`${baseId}-panel-${tab.id}`}`
- Current bug: single panel wrapper uses `id={`${baseId}-panel-${activeTab}`}` and exclusive `activeTab === "..." ? (...) : null` branches (lines ~271–468)
- Content props already include `fairOpportunity` and `programManager` — do not change content modules in this plan
- Nested `PostDeliveryTopics` may keep exclusive topic-description mount (not SEWP-01/02)

Target panel pattern (from 12-RESEARCH):
- Map `TABS` → one `role="tabpanel"` each
- `id={`${baseId}-panel-${tab.id}`}`, `aria-labelledby={`${baseId}-tab-${tab.id}`}`, `hidden={!isActive}`, class `sewp-vi-statements__panel`
- Extract panel body into `renderStatementsPanel(tab.id)` (or equivalent switch) so JSX content is unchanged — only mounting strategy changes
- Do NOT add `aria-hidden` on panels that use `hidden`
- Do NOT wrap each panel in framer-motion remount animations
- Prefer HTML `hidden` over class-only hide (Claude's Discretion resolved by RESEARCH)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Always-mount statement tabpanels with HTML hidden</name>
  <files>components/marketing/NasaSewpViStatementsTableSection.tsx</files>
  <action>
  In `NasaSewpViStatementsTableSection.tsx`, replace the single exclusive-mount tabpanel (current `activeTab ===` branches returning null) with a `TABS.map` that always renders one `role="tabpanel"` per tab.

  Required behavior (SEWP-01, SEWP-02; RESEARCH Pattern 1):
  1. Keep tablist UX: existing keyboard handlers, `aria-selected`, roving `tabIndex`, `setActiveTab` — no visual redesign.
  2. For each tab: stable `id={`${baseId}-panel-${tab.id}`}` matching existing `aria-controls` on the tab button (fix Pitfall 2 — never use a single panel whose id swaps with `activeTab`).
  3. Inactive panels: set HTML boolean attribute `hidden={!isActive}` (not conditional mount, not visually-hidden, not aria-hidden alone).
  4. Active panel: omit `hidden` (`hidden={false}` / attribute absent).
  5. Move each existing panel body into a helper such as `renderStatementsPanel(tabId: TabId)` so Fair Opportunity parsing IIFE, Program Manager profile markup, Obtain Quote embed, etc. stay behavior-identical — only mount strategy changes.
  6. Default `useState<TabId>("contract-overview")` unchanged so SSR and client agree on which panel lacks `hidden` (avoid hydration mismatch).
  7. Do not change Company Hub, nested PostDeliveryTopics mount strategy, content files, or `app/styles/pages/sewp-vi.css` in this plan (RESEARCH: panel class does not set `display`, so UA `[hidden]` works; Plan 02 owns CSS cleanup).
  </action>
  <verify>
    <automated>rg -n "hidden=\{!isActive\}" components/marketing/NasaSewpViStatementsTableSection.tsx; rg -n "activeTab ===" components/marketing/NasaSewpViStatementsTableSection.tsx | rg -v "PostDelivery|activeTopic|caption|isActive|setActiveTab|onTabKeyDown|activeIndex" ; node -e "const fs=require('fs'); const s=fs.readFileSync('components/marketing/NasaSewpViStatementsTableSection.tsx','utf8'); if(!s.includes('hidden={!isActive}')) process.exit(1); if(/activeTab === \"fair-opportunity\" \?/.test(s)) process.exit(2); if(/id=\{`\$\{baseId\}-panel-\$\{activeTab\}`\}/.test(s)) process.exit(3); console.log('panel-mount-ok');"</automated>
  </verify>
  <done>All eight statement panels always render; inactive panels use `hidden`; Fair Opportunity and Program Manager JSX no longer gated by exclusive `activeTab ===` null mounts; tab aria-controls ids remain stable per tab.id.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Fair Opportunity and Program Manager in SSR HTML without tab click</name>
  <files>components/marketing/NasaSewpViStatementsTableSection.tsx</files>
  <action>
  Prove SEWP-01 and SEWP-02 with a no-JS HTML fetch (not a browser tab click).

  Steps:
  1. Run `npm run build` then start production server (`npm run start` on port 3000, or next start equivalent). If a server is already running the latest build on :3000, reuse it.
  2. Fetch HTML with no client interaction: `curl.exe -sL http://localhost:3000/nasa-sewp-vi` (PowerShell-safe).
  3. Assert ALL of the following substrings appear in the response body (case-sensitive SimpleMatch / includes):
     - `FAR Part 16.505` (SEWP-01 Fair Opportunity clause)
     - `Fair Opportunity` (panel/tab copy presence)
     - `Maybelline Magnet` (SEWP-02)
     - `SEWP VI PROGRAM MANAGER` (SEWP-02 role)
     - `(301) 638-8731` (SEWP-02 direct phone)
     - `sewp.pm@abrahamsconsulting.com` (SEWP-02 email)
  4. Assert HTML contains at least 8 `role="tabpanel"` occurrences (one per statement tab).
  5. Assert inactive panels carry the `hidden` attribute in source (e.g. match `hidden` adjacent to tabpanel markup).
  6. Stop the temporary server if this task started it.

  Do not use Playwright or manual tab clicks for acceptance of SEWP-01/02.
  </action>
  <verify>
    <automated>npm run build; $p = Start-Process -FilePath npm -ArgumentList "run","start" -PassThru -WindowStyle Hidden; Start-Sleep -Seconds 8; try { $html = curl.exe -sL http://localhost:3000/nasa-sewp-vi; $need = @("FAR Part 16.505","Fair Opportunity","Maybelline Magnet","SEWP VI PROGRAM MANAGER","(301) 638-8731","sewp.pm@abrahamsconsulting.com"); foreach ($s in $need) { if ($html -notlike "*$s*") { throw "MISSING: $s" } }; if (($html | Select-String -Pattern 'role="tabpanel"' -AllMatches).Matches.Count -lt 8) { throw "EXPECTED at least 8 tabpanels" }; if ($html -notmatch '\shidden(\s|>|/)') { throw "MISSING hidden attr" }; Write-Output "sewp-01-02-html-ok" } finally { Stop-Process -Id $p.Id -Force -ErrorAction SilentlyContinue }</automated>
  </verify>
  <done>Production HTML for /nasa-sewp-vi contains Fair Opportunity and Program Manager Maybelline contact strings without any JavaScript tab interaction; always-mounted hidden tabpanels confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GET `/nasa-sewp-vi` → HTML | Untrusted clients / auditors fetch static marketing HTML |
| Client tab state → DOM `hidden` | Browser toggles visibility; content already public |

## STRIDE Threat Register

| Threat ID | Category | Disposition | Mitigation Plan |
|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure | accept | Always-mount only already-approved public SEWP copy already shipped in content module — no new secrets |
| T-12-02 | Elevation of Privilege (a11y focus) | mitigate | Use real HTML `hidden` so inactive panel links leave focus order; avoid opacity/off-screen hide |
| T-12-SC | Tampering (supply chain) | accept | No new npm packages in this plan |
</threat_model>

<verification>
- `hidden={!isActive}` present; exclusive Fair Opportunity / Program Manager mounts gone
- `npm run build` succeeds
- curl/fetch HTML asserts SEWP-01 and SEWP-02 strings without tab click
</verification>

<success_criteria>
- SEWP-01: Fair Opportunity / FAR Part 16.505 present in initial HTML
- SEWP-02: Maybelline Magnet, role, phone, sewp.pm@ email present in initial HTML
- Tab UX still controls visual focus via `hidden`
</success_criteria>

<output>
Create `.planning/phases/12-sewp-compliance-dom-dead-code/12-01-SUMMARY.md` when done
</output>
