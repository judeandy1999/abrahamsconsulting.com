# Deferred items — Phase 11

Logged during 11-01 execution (out of plan scope / pre-existing).

| Item | Source | Notes |
|------|--------|-------|
| Historical `tests/fixtures/valid-content.json` remains a partial marketing blob (missing utilityLinks and several page sections vs live `marketingContentSchema`) | Task 2 / validator restore | Fixture mode uses CNT-aware site checks; full schema enforced on live `src/content/*.ts` only |
| Unrelated WIP: `nasaSewpBanner` in site/schema + AwardBanner CSS/component edits | Working tree at executor start | Left unstaged; not part of 11-01 commits |
