---
phase: 11-content-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/site.ts
  - src/content/schema.ts
  - components/marketing/MarketingFooter.tsx
  - tests/fixtures/valid-content.json
autonomous: true
requirements:
  - CNT-01
  - CNT-02
must_haves:
  truths:
    - "Contact page Our Office channel shows exactly 30 Broad Street, NY NY 10004 14th Floor (no 40 Wall) (CNT-01)"
    - "Footer address remains 30 Broad Street, NY NY 10004 14th Floor and matches contact (CNT-01)"
    - "SEWP Company Information headquarters stays 30 Broad Street (unchanged if already correct) (locked)"
    - "Footer legal bar has no Privacy Policy link and no new privacy page exists (CNT-02)"
    - "Footer schema and site content no longer define privacyPolicyLabel or privacyPolicyHref (CNT-02)"
    - "npm run content:check and plan03 content-validation fixture tests pass"
  artifacts:
    - path: src/content/site.ts
      provides: "Contact office channel value set to canonical 30 Broad; privacyPolicy* fields removed from footer object"
      contains: "30 Broad Street, NY NY 10004 14th Floor"
    - path: src/content/schema.ts
      provides: "Footer Zod object without privacyPolicyLabel / privacyPolicyHref"
    - path: components/marketing/MarketingFooter.tsx
      provides: "Legal bar with copyright only — no Privacy Policy Link"
    - path: tests/fixtures/valid-content.json
      provides: "Valid fixture aligned to schema without privacyPolicy* fields"
  key_links:
    - from: src/content/site.ts
      to: components/marketing/ContactUsPageContent.tsx
      via: "contactPage.channels office value rendered via ChannelValue"
      pattern: "id:\\s*\"office\"|Our Office"
    - from: src/content/site.ts
      to: components/marketing/MarketingFooter.tsx
      via: "footer.address displayed in Connect column"
      pattern: "footer\\.address"
    - from: src/content/schema.ts
      to: src/content/site.ts
      via: "SiteContent footer type no longer requires privacyPolicy*"
      pattern: "privacyPolicy"
---

<objective>
Align site-wide office address to the footer canonical string and remove the Privacy Policy footer link entirely — without adding a privacy page or touching SEWP Company Info when it already uses 30 Broad.

Purpose: Close CNT-01 and CNT-02 content-consistency gaps from the v1.5 vetting (contact still shows 40 Wall; Privacy Policy links to stub `/about`).
Output: Updated contact content, footer schema/UI without privacy fields, fixture aligned for validation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/content/site.ts
@src/content/schema.ts
@src/content/nasa-sewp-vi.ts
@components/marketing/MarketingFooter.tsx
@components/marketing/ContactUsPageContent.tsx
@tests/fixtures/valid-content.json
@tests/plan03-content-validation.test.mjs

## Locked milestone decisions (honor exactly — no CONTEXT.md)

- **Canonical address:** `30 Broad Street, NY NY 10004 14th Floor` (exact string; match footer).
- **Privacy Policy:** Remove the footer link entirely. Do **not** build a privacy page (CNT-F01 is future-only).
- **SEWP Company Info:** If headquarters is already `30 Broad Street, NY NY 10004 14th Floor`, do **not** edit `src/content/nasa-sewp-vi.ts` — only fix contact if it still shows `40 Wall`.

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | — | One canonical NYC office address; no dead Privacy Policy link | 01 | COVERED | Tasks 1–2 |
| REQ | CNT-01 | Contact office matches footer `30 Broad Street, NY NY 10004 14th Floor` | 01 | COVERED | Task 1 |
| REQ | CNT-02 | Footer no Privacy Policy link; no privacy page | 01 | COVERED | Task 2 |
| RESEARCH | — | N/A (no RESEARCH.md) | — | N/A | Use locked milestone decisions |
| CONTEXT | — | N/A (no CONTEXT.md) | — | N/A | Locked decisions above |
| LOCKED | Address | Canonical 30 Broad Street string | 01 | COVERED | Task 1 |
| LOCKED | Privacy | Remove link; no page | 01 | COVERED | Task 2 |
| LOCKED | SEWP | Leave company info if already 30 Broad | 01 | COVERED | Task 1 verify-only |

## Out of scope for this plan

- Building a Privacy Policy page (CNT-F01 / Out of Scope)
- Phase 12 SEWP DOM / past performance / VPAT work
- Redesigning footer or contact layout
- Editing `nasa-sewp-vi.ts` when Company Information already has the canonical address

## Claude's discretion

- Contact office value may be a single-line string equal to `footer.address` (preferred — exact match). Do not invent a different street format.
- After removing the Privacy Policy link, also remove the `|` legal divider that currently sits between the link and copyright so the bar is copyright-only.
- If `Link` from `next/link` is unused after link removal, drop the import.

<interfaces>
<!-- Executor contracts — extract and use; do not explore for discovery -->

From src/content/site.ts (current — fix these):
- `footer.address` = `"30 Broad Street, NY NY 10004 14th Floor"` (canonical — keep)
- `footer.privacyPolicyLabel` / `footer.privacyPolicyHref` = present → **remove both keys**
- `contactPage.channels` office channel currently:
  - `id: "office"`, `label: "Our Office"`, `value: "40 Wall Street\nNew York, NY"`, `icon: "map-pin"`
  - Set `value` to exactly `"30 Broad Street, NY NY 10004 14th Floor"`

From src/content/schema.ts footer object (current — edit):
- Required today: `privacyPolicyLabel`, `privacyPolicyHref`
- After plan: those two keys **deleted** from the Zod footer object (not made optional — removed)

From src/content/nasa-sewp-vi.ts (verify only):
- `companyInformation.items` headquarters `value` is already `"30 Broad Street, NY NY 10004 14th Floor"` — do not modify file if still true

From components/marketing/MarketingFooter.tsx (current legal bar):
- Renders `<Link href={footer.privacyPolicyHref}>{footer.privacyPolicyLabel}</Link>` then `|` divider then copyright
- After: copyright span only (no Privacy Link, no divider)

From components/marketing/ContactUsPageContent.tsx:
- `ChannelValue` already splits on `\n` — no component change required if office value is updated in content only

From tests/fixtures/valid-content.json:
- Contains `privacyPolicyLabel` / `privacyPolicyHref` — must remove when schema drops those fields or `plan03-content-validation` fails
- Fixture office value may be updated to the canonical string for consistency (optional but preferred)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align contact office address to canonical 30 Broad</name>
  <files>src/content/site.ts, tests/fixtures/valid-content.json</files>
  <action>
  Implement CNT-01 per locked milestone decision (canonical address `30 Broad Street, NY NY 10004 14th Floor`).

  1. In `src/content/site.ts`, find `contactPage.channels` entry with `id: "office"` and set `value` to exactly `30 Broad Street, NY NY 10004 14th Floor` (same string as `footer.address`). Remove any `40 Wall` / `Wall Street` text from contact content.
  2. Confirm `footer.address` is already `30 Broad Street, NY NY 10004 14th Floor` — do not change it unless it drifted.
  3. Confirm `src/content/nasa-sewp-vi.ts` `companyInformation` headquarters value is already the canonical 30 Broad string. If yes, **do not edit** that file. If somehow still `40 Wall`, only then update that headquarters value to the canonical string (locked: fix contact; leave SEWP alone when already correct).
  4. In `tests/fixtures/valid-content.json`, set the office channel `value` to the same canonical string (single line preferred) so fixtures stay consistent with production content.
  5. Do not create a privacy page. Do not change contact layout components unless a type error forces a trivial fix (none expected).
  </action>
  <verify>
    <automated>rg -n "40 Wall|Wall Street" src/content/site.ts components; rg -n "30 Broad Street, NY NY 10004 14th Floor" src/content/site.ts src/content/nasa-sewp-vi.ts</automated>
  </verify>
  <done>Contact office channel value equals footer address string; no `40 Wall` / `Wall Street` remains in `src/content/site.ts`; SEWP headquarters still shows 30 Broad; fixture office value aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Privacy Policy footer link end-to-end</name>
  <files>src/content/schema.ts, src/content/site.ts, components/marketing/MarketingFooter.tsx, tests/fixtures/valid-content.json</files>
  <action>
  Implement CNT-02 per locked milestone decision (remove Privacy Policy footer link; do not build a page).

  1. In `src/content/schema.ts`, delete `privacyPolicyLabel` and `privacyPolicyHref` from the `footer` Zod object entirely (not optional — removed). Keep all other footer fields including `copyrightName`.
  2. In `src/content/site.ts`, delete `privacyPolicyLabel` and `privacyPolicyHref` from the `footer` object.
  3. In `components/marketing/MarketingFooter.tsx`:
     - Remove the Privacy Policy `<Link>` render.
     - Remove the `|` legal divider between the former link and copyright.
     - Leave the copyright span (`copyrightName` + year + All Rights Reserved).
     - Remove unused `Link` import from `next/link` if nothing else in the file uses it.
  4. In `tests/fixtures/valid-content.json`, delete `privacyPolicyLabel` and `privacyPolicyHref` from the footer object so the valid fixture matches the new schema.
  5. Grep production code (exclude `.planning/`) for `privacyPolicy` — zero remaining references in `src/`, `components/`, `app/`, `lib/`, `tests/`. Historical mentions under `.planning/` may remain.
  6. Do **not** add `app/(marketing)/privacy` or any privacy route/page.
  </action>
  <verify>
    <automated>rg -n "privacyPolicy|Privacy Policy" src components app lib tests --glob "!.planning/**"; npm run content:check; node --test tests/plan03-content-validation.test.mjs</automated>
  </verify>
  <done>No privacyPolicy fields in schema/content/footer UI/fixtures; footer legal bar shows copyright only; no privacy page added; `npm run content:check` and `node --test tests/plan03-content-validation.test.mjs` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public marketing pages → visitors | Published address and legal links are trusted as official company facts |
| Content modules → rendered HTML | Typed `site.ts` / schema gate what the footer and contact page display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `contactPage` office channel | mitigate | Replace stale `40 Wall` with canonical `30 Broad Street, NY NY 10004 14th Floor` matching footer (CNT-01) |
| T-11-02 | Spoofing / Elevation of Privilege (trust) | Footer Privacy Policy → `/about` stub | mitigate | Remove privacyPolicy link/fields entirely; do not ship a fake privacy page (CNT-02) |
| T-11-03 | Tampering | `nasa-sewp-vi` company address | accept | Already canonical 30 Broad; leave unchanged per locked decision |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `rg` finds no `40 Wall` / `Wall Street` under `src/content/site.ts` (and no production contact address drift)
- Contact + footer + SEWP headquarters all present the canonical 30 Broad string
- `rg` finds no `privacyPolicy` / `Privacy Policy` under `src/`, `components/`, `app/`, `lib/`, `tests/`
- No new privacy route under `app/`
- `npm run content:check` exits 0
- `node --test tests/plan03-content-validation.test.mjs` exits 0
</verification>

<success_criteria>
- CNT-01: Contact Our Office matches footer `30 Broad Street, NY NY 10004 14th Floor`; no `40 Wall` on contact
- CNT-02: Footer has no Privacy Policy link; schema/content/fixtures cleaned; no privacy page introduced
- Content validation green
</success_criteria>

<output>
Create `.planning/phases/11-content-consistency/11-01-SUMMARY.md` when done
</output>
