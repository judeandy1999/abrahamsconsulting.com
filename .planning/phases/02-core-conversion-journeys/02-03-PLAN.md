---
phase: 02-core-conversion-journeys
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/(marketing)/layout.tsx
  - app/(marketing)/services/page.tsx
  - app/(marketing)/services/[slug]/page.tsx
  - app/(marketing)/contracts/page.tsx
  - tests/plan02-services-contracts-journey.test.mjs
autonomous: true
requirements:
  - IA-02
  - IA-03
  - LEAD-01
must_haves:
  truths:
    - "Users can navigate from services overview to specific capability pages."
    - "Users can discover contract vehicles from primary navigation and contextual links."
    - "Key service and procurement pages expose the same consultation CTA destination."
  artifacts:
    - path: "app/(marketing)/services/[slug]/page.tsx"
      provides: "Capability detail journey pages generated from typed service content"
    - path: "app/(marketing)/contracts/page.tsx"
      provides: "Procurement-discovery page with contract vehicle clarity and CTA"
    - path: "app/(marketing)/layout.tsx"
      provides: "Primary navigation exposing service/procurement discovery paths"
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "app/(marketing)/contracts/page.tsx"
      via: "primary navigation link"
      pattern: "href=\"/contracts\""
    - from: "app/(marketing)/services/page.tsx"
      to: "app/(marketing)/services/[slug]/page.tsx"
      via: "capability detail links"
      pattern: "href={`/services/"
    - from: "app/(marketing)/services/[slug]/page.tsx"
      to: "/consultation"
      via: "service-level consultation CTA"
      pattern: "href=\"/consultation\""
---

<objective>
Implement service and procurement conversion journeys with explicit internal linking and shared consultation CTA routing.

Purpose: Deliver `IA-02`, `IA-03`, and service/contracts-side `LEAD-01` behavior by expanding route depth and cross-link discoverability.
Output: Service index + capability detail pages, procurement-focused contracts page, navigation updates, and automated journey regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/02-core-conversion-journeys/02-core-conversion-journeys-CONTEXT.md
@.planning/phases/02-core-conversion-journeys/02-01-PLAN.md
@app/(marketing)/layout.tsx
@app/(marketing)/services/page.tsx
@app/(marketing)/contracts/page.tsx

<interfaces>
From `src/content/services.ts`:
- `servicesContent: ServiceItem[]` (slug, title, summary + Phase 2 contract fields from Plan 02-01)

From `lib/content/load-content.ts`:
- `loadMarketingContent()` returns service and contract datasets for route rendering.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define services/contracts journey contract tests</name>
  <files>tests/plan02-services-contracts-journey.test.mjs</files>
  <behavior>
    - Services overview links to capability detail pages generated by service slug.
    - Contracts and service pages include links to `/consultation` and procurement-discovery paths.
    - Primary navigation exposes `/services` and `/contracts` routes for discoverability.
  </behavior>
  <action>Author source-level tests for `IA-02`, `IA-03`, and `LEAD-01` journey contracts before route updates. Keep tests deterministic and focused on route/link contracts rather than exact prose strings.</action>
  <verify>
    <automated>node --test tests/plan02-services-contracts-journey.test.mjs</automated>
  </verify>
  <done>Journey tests fail initially and identify missing capability-linking and CTA contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build capability and procurement route slices</name>
  <files>app/(marketing)/services/page.tsx, app/(marketing)/services/[slug]/page.tsx, app/(marketing)/contracts/page.tsx</files>
  <behavior>
    - `/services` renders clear capability summaries with links to `/services/[slug]`.
    - `/services/[slug]` resolves static params from typed service content and includes consultation CTA.
    - `/contracts` presents procurement-relevant positioning with internal links to services/consultation.
  </behavior>
  <action>Implement route updates per `D-02` and `D-03` using typed content loader data, including static service detail generation via slug-based params. Keep pages static-first and avoid runtime API dependencies or out-of-scope lead form handling.</action>
  <verify>
    <automated>node --test tests/plan02-services-contracts-journey.test.mjs</automated>
  </verify>
  <done>Service/procurement journey tests pass with clear capability routing and consultation CTA wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Wire navigation discoverability and run quality gates</name>
  <files>app/(marketing)/layout.tsx, tests/plan02-services-contracts-journey.test.mjs</files>
  <action>Update primary navigation to reinforce service/procurement discoverability for `IA-03` and validate all route/journey tests with lint/build checks. Keep navigation server-rendered and avoid adding client-side state for static links.</action>
  <verify>
    <automated>node --test tests/plan02-services-contracts-journey.test.mjs && npm run lint && npm run build</automated>
  </verify>
  <done>Primary navigation and supporting journey routes pass automated checks and remain production-build safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public user->service/procurement routes | Untrusted traffic traverses navigation and internal journey links. |
| content routing data->dynamic segment generation | Service slug content controls generated capability route paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | service slug route generation | mitigate | Generate static params from validated typed service content and assert via tests. |
| T-02-08 | Denial of Service | route rendering mode | mitigate | Keep services/contracts routes static-first with no request-time dependency added. |
| T-02-09 | Spoofing | CTA destination consistency | mitigate | Reuse canonical `/consultation` path across services/contracts and enforce in tests. |
| T-02-SC | Tampering | dependency supply chain | mitigate | No new package installs in this plan; stay within existing audited dependencies. |
</threat_model>

<verification>
Run:
- `node --test tests/plan02-services-contracts-journey.test.mjs`
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Users can traverse services overview to capability detail pages.
- Contract vehicles are discoverable from primary navigation and contextual links.
- Services and contracts pages consistently route consultation intent to `/consultation`.
</success_criteria>

<output>
Create `.planning/phases/02-core-conversion-journeys/02-03-SUMMARY.md` when done
</output>
