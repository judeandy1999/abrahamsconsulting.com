---
phase: 02-core-conversion-journeys
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/(marketing)/page.tsx
  - tests/plan02-home-conversion-journey.test.mjs
autonomous: true
requirements:
  - IA-01
  - LEAD-01
must_haves:
  truths:
    - "Users can immediately understand what Abrahams Consulting offers from Home hero and primary sections."
    - "Users can reach the single consultation destination from Home CTA actions."
  artifacts:
    - path: "app/(marketing)/page.tsx"
      provides: "Conversion-ready home narrative with clear value proposition and CTA hierarchy"
    - path: "tests/plan02-home-conversion-journey.test.mjs"
      provides: "Automated checks for Home value proposition and CTA destination contracts"
  key_links:
    - from: "app/(marketing)/page.tsx"
      to: "lib/content/load-content.ts"
      via: "typed content loader for hero and section copy"
      pattern: "loadMarketingContent"
    - from: "app/(marketing)/page.tsx"
      to: "/consultation"
      via: "primary CTA link"
      pattern: "href=\"/consultation\""
---

<objective>
Implement the Home conversion slice with value-proposition clarity and direct consultation routing.

Purpose: Deliver `IA-01` and Home-side `LEAD-01` behavior by transforming the starter Home page into a structured conversion journey wired to typed content.
Output: Home page with clear offer messaging, supporting sections, and a primary CTA to `/consultation` backed by automated contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-conversion-journeys/02-core-conversion-journeys-CONTEXT.md
@.planning/phases/02-core-conversion-journeys/02-01-PLAN.md
@lib/content/load-content.ts
@app/(marketing)/page.tsx

<interfaces>
From `lib/content/load-content.ts`:
- `export function loadMarketingContent(): MarketingContent`

From `src/content/schema.ts`:
- `MarketingContent.site`
- `MarketingContent.services`
- `MarketingContent.contracts`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Home conversion contract tests</name>
  <files>tests/plan02-home-conversion-journey.test.mjs</files>
  <behavior>
    - Home route contains one primary value proposition heading and supporting summary section for offer clarity.
    - Home route contains a primary CTA link targeting `/consultation` per `D-03`.
  </behavior>
  <action>Create deterministic source-level tests for Home conversion requirements (`IA-01`, `LEAD-01`) before implementation changes. Keep assertions focused on contract markers (section identifiers, CTA href) rather than brittle full-copy matching.</action>
  <verify>
    <automated>node --test tests/plan02-home-conversion-journey.test.mjs</automated>
  </verify>
  <done>Tests initially fail against current Home structure and clearly describe missing conversion contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Home value-proposition and CTA sections</name>
  <files>app/(marketing)/page.tsx</files>
  <behavior>
    - Home hero and primary sections communicate consulting/staffing/procurement value proposition clearly.
    - Primary CTA and at least one secondary journey link route users to high-intent next steps.
  </behavior>
  <action>Refactor Home page per `D-02` and `D-03` to load typed content, render a clear value proposition hierarchy, and include a primary CTA linking to `/consultation` plus secondary links to services/contracts for journey branching. Keep route static-first and server-component native.</action>
  <verify>
    <automated>node --test tests/plan02-home-conversion-journey.test.mjs</automated>
  </verify>
  <done>Home conversion tests pass and page preserves static-first build behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Validate integrated Home conversion slice</name>
  <files>app/(marketing)/page.tsx, tests/plan02-home-conversion-journey.test.mjs</files>
  <action>Run integration checks to ensure Home conversion contracts and overall project quality gates pass together after implementation. Do not weaken existing lint/type/build gates to make tests pass.</action>
  <verify>
    <automated>node --test tests/plan02-home-conversion-journey.test.mjs && npm run lint && npm run build</automated>
  </verify>
  <done>Home conversion behavior is validated and repository quality gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed content->Home rendering | Marketing copy and links cross from content modules into user-facing conversion UI. |
| user->Home CTA navigation | Untrusted users trigger CTA transitions to consultation and journey routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Home CTA destination wiring | mitigate | Require source-level tests asserting `/consultation` CTA target and fail on regression. |
| T-02-05 | Denial of Service | Home route rendering path | mitigate | Keep Home route static-first with no runtime dependency introduced in conversion refactor. |
| T-02-06 | Repudiation | conversion requirement coverage | mitigate | Bind requirement contracts to automated tests and retain CI lint/build evidence. |
| T-02-SC | Tampering | package/dependency drift | mitigate | Implement using existing stack only; no new installs required for this slice. |
</threat_model>

<verification>
Run:
- `node --test tests/plan02-home-conversion-journey.test.mjs`
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- Home page communicates offer clarity through explicit section structure and typed content.
- Home provides a primary consultation CTA that routes to `/consultation`.
- Automated tests and quality gates pass with no regression to static-first behavior.
</success_criteria>

<output>
Create `.planning/phases/02-core-conversion-journeys/02-02-SUMMARY.md` when done
</output>
