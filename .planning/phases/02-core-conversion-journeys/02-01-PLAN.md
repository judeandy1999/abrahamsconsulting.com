---
phase: 02-core-conversion-journeys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/schema.ts
  - src/content/site.ts
  - src/content/services.ts
  - src/content/contracts.ts
  - app/(marketing)/consultation/page.tsx
  - app/sitemap.ts
  - tests/plan02-consultation-route-contract.test.mjs
autonomous: true
requirements:
  - IA-03
  - LEAD-01
must_haves:
  truths:
    - "Users have one explicit consultation destination reachable by URL."
    - "Core marketing content includes CTA and procurement link fields needed by downstream pages."
  artifacts:
    - path: "app/(marketing)/consultation/page.tsx"
      provides: "Single consultation destination route used by CTA links"
    - path: "src/content/schema.ts"
      provides: "Typed conversion-journey contracts for CTA and service/procurement linking"
    - path: "tests/plan02-consultation-route-contract.test.mjs"
      provides: "Automated contract checks for consultation route and sitemap inclusion"
  key_links:
    - from: "src/content/schema.ts"
      to: "src/content/services.ts"
      via: "typed parsing for service capability CTA fields"
      pattern: "z\\.object\\(|ServiceItem"
    - from: "app/sitemap.ts"
      to: "app/(marketing)/consultation/page.tsx"
      via: "route list entry for consultation path"
      pattern: "/consultation"
---

<objective>
Create Phase 2 interface contracts and consultation-route foundation so all conversion pages can wire to one clear CTA destination.

Purpose: Implement `D-02` and `D-03` while preparing content and route contracts that Phase 2 feature slices consume without ambiguity.
Output: Updated typed content contracts, consultation route, and route-level test coverage proving destination/link discoverability.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/02-core-conversion-journeys/02-core-conversion-journeys-CONTEXT.md

<interfaces>
From `src/content/schema.ts`:
- `export type SiteContent`
- `export type ServiceItem`
- `export type ContractItem`
- `export const marketingContentSchema`

From `app/sitemap.ts`:
- `const marketingRoutes = ["/", "/about", "/services", "/contracts"]`
- `export default function sitemap(): MetadataRoute.Sitemap`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend typed conversion contracts</name>
  <files>src/content/schema.ts, src/content/site.ts, src/content/services.ts, src/content/contracts.ts</files>
  <behavior>
    - Content schema enforces one primary consultation CTA destination (path + label) per `D-03`.
    - Service entries expose fields needed for capability routing and procurement-link visibility per `D-02`.
  </behavior>
  <action>Add schema and content fields for conversion journey contracts per `D-02` and `D-03` (consultation CTA metadata, service capability link intent, procurement cross-link text). Keep files as typed data modules only and do not introduce executable business logic in content modules.</action>
  <verify>
    <automated>node scripts/validate-content.mjs</automated>
  </verify>
  <done>Updated content modules validate successfully and expose typed fields consumed by Phase 2 pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add consultation destination route and crawl inclusion</name>
  <files>app/(marketing)/consultation/page.tsx, app/sitemap.ts</files>
  <behavior>
    - `/consultation` builds as a static marketing route and renders consultation next-step guidance.
    - Sitemap output includes `/consultation` for crawler discoverability.
  </behavior>
  <action>Create a static consultation page per `D-03` and include the path in sitemap generation so CTA links resolve to a canonical destination discoverable by users and crawlers. Keep route server-component first and avoid runtime fetch dependency.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Consultation route exists, builds successfully, and appears in generated sitemap output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock consultation route contracts with automated tests</name>
  <files>tests/plan02-consultation-route-contract.test.mjs</files>
  <behavior>
    - Test fails when consultation route export/path is removed.
    - Test fails when sitemap omits consultation path.
  </behavior>
  <action>Add source-level route contract tests proving consultation destination presence per `D-03` and sitemap linkage for `IA-03`. Keep assertions deterministic and file-based (no network calls).</action>
  <verify>
    <automated>node --test tests/plan02-consultation-route-contract.test.mjs</automated>
  </verify>
  <done>Automated tests pass and fail deterministically if consultation route or sitemap entry regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content modules->rendered marketing pages | Authored content crosses into public conversion messaging. |
| user navigation->consultation route | Untrusted user traffic hits CTA destination route directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/content/*.ts` | mitigate | Enforce schema validation through `node scripts/validate-content.mjs` before build. |
| T-02-02 | Spoofing | consultation CTA destination | mitigate | Use one canonical in-repo consultation path and assert with route contract tests. |
| T-02-03 | Information Disclosure | consultation page content | mitigate | Keep page content static and avoid environment-secret interpolation in rendered output. |
| T-02-SC | Tampering | npm installs in Phase 2 implementation | mitigate | Do not add packages unless audited in research; keep implementation within existing dependencies. |
</threat_model>

<verification>
Run:
- `node scripts/validate-content.mjs`
- `node --test tests/plan02-consultation-route-contract.test.mjs`
- `npm run build`
</verification>

<success_criteria>
- Consultation destination route exists and is crawl-discoverable.
- Typed content contracts provide CTA and cross-link fields required by downstream Phase 2 pages.
- Automated tests detect consultation-route and sitemap regressions.
</success_criteria>

<output>
Create `.planning/phases/02-core-conversion-journeys/02-01-SUMMARY.md` when done
</output>
