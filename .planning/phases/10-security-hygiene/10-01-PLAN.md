---
phase: 10-security-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - app/api/lead/route.ts
  - lib/lead/send-lead-email.ts
  - tests/plan03-lead-capture-flow.test.mjs
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Developer confirms .env* is gitignored while .env.example remains tracked and commitable (SEC-01)"
    - "POST /api/lead no longer exists (404 or missing route); contact remains HubSpot-only (SEC-02)"
    - "Resend/lead notification env vars are gone from .env.example; HubSpot contact UI and success page remain (SEC-02)"
    - "plan03 lead-capture tests pass after asserting route/helper absence and HubSpot-only path"
  artifacts:
    - path: .gitignore
      provides: ".env* ignore rule with !.env.example exception"
      contains: ".env*"
    - path: .env.example
      provides: "Public env template without RESEND_/LEAD_NOTIFICATION_ secrets"
    - path: tests/plan03-lead-capture-flow.test.mjs
      provides: "HubSpot-only + deleted-lead-API assertions"
  key_links:
    - from: .gitignore
      to: .env.example
      via: "!.env.example negation after .env*"
      pattern: "\\.env\\*|!\\.env\\.example"
    - from: components/marketing/ContactUsPageContent.tsx
      to: components/marketing/HubSpotContactForm.tsx
      via: "sole contact submit path (must remain)"
      pattern: "HubSpotContactForm"
    - from: tests/plan03-lead-capture-flow.test.mjs
      to: app/api/lead/route.ts
      via: "assert file absent after delete"
      pattern: "access|ENOENT|does not exist|absent"
---

<objective>
Close secret-commit and orphan-API spam gaps: ignore `.env*` while keeping `.env.example` tracked, and delete the unused Resend-backed `POST /api/lead` stack so contact stays HubSpot-only.

Purpose: Satisfy SEC-01 and SEC-02 from the v1.5 security hygiene milestone without touching Next pins or headers (owned by plan 02).
Output: Updated `.gitignore`, cleaned `.env.example`, deleted lead route/helper, rewritten plan03 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-security-hygiene/10-RESEARCH.md
@.gitignore
@.env.example
@app/api/lead/route.ts
@lib/lead/send-lead-email.ts
@tests/plan03-lead-capture-flow.test.mjs
@components/marketing/HubSpotContactForm.tsx
@components/marketing/ContactUsPageContent.tsx

## Locked decisions (honor exactly)

- SEC-01: Ignore `.env*` and keep `.env.example` tracked (negation after the glob).
- SEC-02: Delete `/api/lead` + Resend lead helper; HubSpot-only contact — do not leave a disabled stub route.
- Do not expand scope into CSP/HSTS or Next version pins (plan 02).

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Repo no longer exposes common secret/spam-risk gaps | 01+02 | COVERED |
| REQ | SEC-01 | Ignore `.env*` keep `.env.example` | 01 | COVERED |
| REQ | SEC-02 | Remove orphan lead API; HubSpot-only | 01 | COVERED |
| RESEARCH | — | `.env*` + `!.env.example` pattern | 01 | COVERED |
| RESEARCH | — | Delete route + helper; rewrite plan03 tests; strip Resend env | 01 | COVERED |
| LOCKED | — | Delete `/api/lead`; HubSpot-only (PROJECT.md) | 01 | COVERED |

## Out of scope for this plan

- SEC-03 / SEC-04 (Next pin, HSTS, CSP) → 10-02
- Full `"latest"` pin cleanup beyond Next → deferred per REQUIREMENTS
- Nonce-based CSP / new packages / CRM wiring

<interfaces>
<!-- Executor contracts — use these paths/behaviors; do not recreate lead API -->

From tests/plan03-lead-capture-flow.test.mjs (current — rewrite lead assertions):
- Keep HubSpot embed tests (ContactUsPageContent + HubSpotContactForm + js.hsforms.net).
- Keep success-page confirmation test.
- Replace lead-API / Resend / env-Resend tests with: files `app/api/lead/route.ts` and `lib/lead/send-lead-email.ts` must be absent; `.env.example` must NOT document RESEND_API_KEY / LEAD_NOTIFICATION_*.

From components/marketing/ContactUsPageContent.tsx:
- Renders `HubSpotContactForm` with `contactPage.hubspotForm` — do not change contact UI path.

Delete exactly:
- `app/api/lead/route.ts`
- `lib/lead/send-lead-email.ts`
- Empty `app/api/lead/` and `lib/lead/` directories if empty after delete
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gitignore .env* with .env.example exception</name>
  <files>.gitignore</files>
  <action>
  Implement SEC-01 per locked decision and 10-RESEARCH Pattern 1.

  Append to `.gitignore` (after existing entries) a secrets block:
  - Comment clarifying local secrets stay untracked while the example template remains tracked
  - Rule `.env*`
  - Exception `!.env.example` immediately after `.env*` (order matters — negation after the ignore glob)

  Do not rename or untrack `.env.example`. Do not add other ignore rules. Do not commit any real `.env` / `.env.local` files.
  </action>
  <verify>
    <automated>git check-ignore -v .env.local; git check-ignore -v .env.example; git ls-files --error-unmatch .env.example</automated>
  </verify>
  <done>`.env.local` (and other `.env*` variants) match ignore rules; `git check-ignore` does not treat `.env.example` as ignored; `.env.example` remains in the index (`git ls-files`).</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphan lead API and retarget plan03 tests</name>
  <files>app/api/lead/route.ts, lib/lead/send-lead-email.ts, .env.example, tests/plan03-lead-capture-flow.test.mjs</files>
  <action>
  Implement SEC-02 per locked PROJECT.md decision (delete `/api/lead` — HubSpot-only) and 10-RESEARCH Pattern 3.

  1. Delete `app/api/lead/route.ts` and `lib/lead/send-lead-email.ts`. Remove empty `app/api/lead/` and `lib/lead/` directories if they remain empty. Do not leave a stub route, disabled handler, or redirect for later.
  2. Edit `.env.example`: remove `RESEND_API_KEY`, `LEAD_NOTIFICATION_TO`, `LEAD_NOTIFICATION_FROM` and their comments. Keep `NEXT_PUBLIC_SITE_URL` and `NEXT_PUBLIC_GA_MEASUREMENT_ID` (and any other non-Resend public vars).
  3. Rewrite `tests/plan03-lead-capture-flow.test.mjs`:
     - Keep assertions that contact page embeds HubSpot (`HubSpotContactForm`, portal/form/region, `js.hsforms.net/forms/embed/v2.js`).
     - Keep success-page confirmation assertions.
     - Replace lead-API and Resend-adapter tests with assertions that `app/api/lead/route.ts` and `lib/lead/send-lead-email.ts` do not exist (e.g. `access`/`stat` fails with ENOENT, or equivalent).
     - Replace env-example Resend documentation test with assertions that `.env.example` does NOT contain `RESEND_API_KEY`, `LEAD_NOTIFICATION_TO`, or `LEAD_NOTIFICATION_FROM`.
  4. Do not modify `HubSpotContactForm.tsx` or `ContactUsPageContent.tsx` unless a stale import of deleted lead modules appears (grep should show none in production UI).
  5. Grep the repo for remaining production imports of `send-lead-email` or `/api/lead` outside planning docs and remove any broken code imports if found (planning/history docs may still mention the old path — leave `.planning/` alone unless a build fails).
  </action>
  <verify>
    <automated>node --test tests/plan03-lead-capture-flow.test.mjs</automated>
  </verify>
  <done>Lead route and Resend helper files are gone; `.env.example` has no Resend/lead-notification vars; HubSpot contact path unchanged; `node --test tests/plan03-lead-capture-flow.test.mjs` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → git remote | Local `.env*` secrets must not enter the repository |
| Public internet → `/api/*` | Orphan POST handlers are unauthenticated spam/probe surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `.env*` / git | mitigate | `.env*` gitignore + tracked `.env.example` only (SEC-01) |
| T-10-02 | Denial of Service / Abuse | `app/api/lead/route.ts` | mitigate | Delete orphan POST route and Resend helper; HubSpot-only contact (SEC-02) |
| T-10-03 | Information Disclosure | `.env.example` | mitigate | Remove RESEND_/LEAD_NOTIFICATION_ placeholders so example does not imply server secret wiring for a deleted path |
| T-10-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `git check-ignore -v .env.local` shows ignore match; `.env.example` is not ignored and is tracked
- `Test-Path app/api/lead/route.ts` / `lib/lead/send-lead-email.ts` are false
- `node --test tests/plan03-lead-capture-flow.test.mjs` exits 0
</verification>

<success_criteria>
- SEC-01 observable: secrets glob ignored, example tracked
- SEC-02 observable: lead API absent; HubSpot contact path + updated tests green
</success_criteria>

<output>
Create `.planning/phases/10-security-hygiene/10-01-SUMMARY.md` when done
</output>
