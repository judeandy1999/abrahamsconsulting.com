---
phase: 10-security-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
autonomous: false
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "App runs on Next.js 16.2.12 (or newer patch on the same line) with production build succeeding (SEC-03)"
    - "Production responses include Strict-Transport-Security (SEC-04)"
    - "Production responses include enforce-mode Content-Security-Policy allowing HubSpot, GA, YouTube, and blog embed (SEC-04)"
    - "Marketing pages home, contact-us, solutions, and blog load without CSP console blocks for required third parties"
  artifacts:
    - path: package.json
      provides: "next and eslint-config-next pinned to 16.2.12 (not latest)"
      contains: "16.2.12"
    - path: package-lock.json
      provides: "Lockfile resolves next@16.2.12"
    - path: next.config.ts
      provides: "HSTS + enforce allowlist CSP via headers()"
      contains: "Strict-Transport-Security"
  key_links:
    - from: next.config.ts
      to: "all HTML responses via /(.*) headers"
      via: "async headers()"
      pattern: "Content-Security-Policy|Strict-Transport-Security"
    - from: next.config.ts CSP
      to: "HubSpot / GA / YouTube / blog iframe origins"
      via: "script-src connect-src frame-src form-action allowlists"
      pattern: "hsforms|googletagmanager|youtube|abrahams73\\.lll-ll\\.com"
    - from: package.json
      to: package-lock.json
      via: "npm install next@16.2.12 eslint-config-next@16.2.12"
      pattern: "next@16\\.2\\.12"
---

<objective>
Pin Next.js (and eslint-config-next) to the patched 16.2.12 line and ship enforce-mode HSTS + allowlist CSP from `next.config.ts` without breaking HubSpot, GA, YouTube, or the blog embed.

Purpose: Satisfy SEC-03 and SEC-04 transport/framework hardening while preserving static marketing performance (no nonce/proxy CSP).
Output: Pinned package.json/lockfile, extended security headers in next.config.ts, preview smoke confirmation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-security-hygiene/10-RESEARCH.md
@package.json
@next.config.ts
@app/layout.tsx
@components/marketing/HubSpotContactForm.tsx
@components/marketing/GoogleAnalytics.tsx
@src/content/blog.ts

## Locked decisions (honor exactly)

- Pin `next` and `eslint-config-next` to `16.2.12` (exact). Do not expand `"latest"` cleanup to react/zod/typescript/other packages.
- Add HSTS + allowlist CSP in `next.config.ts` (not nonce-based / not proxy.ts).
- Enforce CSP (header `Content-Security-Policy`, not Report-Only) with preview smoke.
- Allow HubSpot, Google Analytics, YouTube, and blog embed (`https://abrahams73.lll-ll.com`).

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Responses ship hardened transport/content security headers | 02 | COVERED |
| REQ | SEC-03 | Next patched ≥ 16.2.12 | 02 | COVERED |
| REQ | SEC-04 | HSTS + staged CSP for HubSpot/GA/YouTube/blog | 02 | COVERED |
| RESEARCH | — | `npm install next@16.2.12 eslint-config-next@16.2.12` | 02 | COVERED |
| RESEARCH | — | Config CSP + unsafe-inline; isDev unsafe-eval + SI hosts | 02 | COVERED |
| RESEARCH | — | Enforce CSP; preview smoke gate | 02 | COVERED |
| LOCKED | — | No other latest pins; no nonce CSP | 02 | COVERED |

## Out of scope for this plan

- SEC-01 / SEC-02 (gitignore + lead API) → 10-01
- Nonce CSP / middleware proxy CSP
- Expanding CSP for Google Ads / doubleclick unless preview proves required
- Pinning unrelated `"latest"` packages

<interfaces>
<!-- Current next.config.ts headers baseline — extend, do not remove -->

Existing headers on source `/(.*)`:
- X-Content-Type-Options: nosniff
- X-Frame-Options: DENY
- Referrer-Policy: strict-origin-when-cross-origin
- Permissions-Policy: camera=(), microphone=(), geolocation=()
- poweredByHeader: false (keep)

Add:
- Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
- Content-Security-Policy: enforce allowlist string from 10-RESEARCH Pattern 2

CSP must include (from research):
- default-src 'self'
- script-src 'self' 'unsafe-inline' (+ 'unsafe-eval' when NODE_ENV===development) + hsforms + googletagmanager
- style-src 'self' 'unsafe-inline' + hsforms hosts
- img-src 'self' data: blob: + ytimg + hsforms + gtag/GA hosts
- font-src 'self' data:
- connect-src 'self' + hsforms/hubapi + gtag/GA (+ va.vercel-scripts.com / vitals.vercel-insights.com when isDev)
- frame-src youtube + hsforms + https://abrahams73.lll-ll.com
- child-src hsforms; object-src 'none'; base-uri 'self'
- form-action 'self' + hsforms/hubspot; frame-ancestors 'none'; upgrade-insecure-requests

package.json today:
- "next": "latest", "eslint-config-next": "latest" → change both to "16.2.12"
- Leave react/react-dom/zod/typescript/etc. on their current pins
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Next.js and eslint-config-next to 16.2.12</name>
  <files>package.json, package-lock.json</files>
  <action>
  Implement SEC-03 per locked decision and 10-RESEARCH installation baseline.

  1. Confirm registry target at execute time with `npm view next version` (expect 16.2.12 or newer patch on 16.2.x). Install/pin exactly `16.2.12` unless a newer 16.2.x patch is required to clear advisories — do not jump major/minor.
  2. Run `npm install next@16.2.12 eslint-config-next@16.2.12`.
  3. Ensure `package.json` lists `"next": "16.2.12"` under dependencies and `"eslint-config-next": "16.2.12"` under devDependencies (not `"latest"`). Do not change other `"latest"` pins (react, zod, typescript, @types/*, eslint) per locked scope.
  4. Commit lockfile changes with the plan work. Verify with `npm ls next eslint-config-next` that both resolve to 16.2.12.
  5. Run `npm run typecheck`, `npm run lint`, and `npm run build` and fix only breakages caused by the patch upgrade (expected low risk 16.2.6→16.2.12).
  </action>
  <verify>
    <automated>npm ls next eslint-config-next --depth=0 && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>`next` and `eslint-config-next` are pinned and locked at 16.2.12; typecheck, lint, and production build succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Add HSTS and enforce allowlist CSP headers</name>
  <files>next.config.ts</files>
  <read_first>next.config.ts, .planning/phases/10-security-hygiene/10-RESEARCH.md</read_first>
  <action>
  Implement SEC-04 per locked decisions and 10-RESEARCH Pattern 2 (config CSP without nonces).

  1. In `next.config.ts`, before `nextConfig`, build a `contentSecurityPolicy` string from the research allowlist (join directives with `"; "`). Use `const isDev = process.env.NODE_ENV === "development"` to append `'unsafe-eval'` on script-src and Vercel Speed Insights dev hosts on connect-src/script-src as documented in research Pitfall 4.
  2. Extend the existing `headers()` block for `source: "/(.*)"` — keep all four current headers unchanged. Append:
     - `{ key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains; preload" }`
     - `{ key: "Content-Security-Policy", value: contentSecurityPolicy }` (enforce — NOT Report-Only)
  3. Do not introduce `proxy.ts` / middleware nonces. Do not remove `poweredByHeader: false` or image remotePatterns.
  4. After edit, run `npm run build`. Start the production server, fetch the home document headers, and assert both `strict-transport-security` and `content-security-policy` are present and CSP mentions hsforms, googletagmanager, youtube, and abrahams73.lll-ll.com. Stop the server when done.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`next.config.ts` ships HSTS + enforce CSP allowlist; build succeeds; response headers include both security headers with required third-party origins in CSP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Preview CSP smoke on marketing pages</name>
  <files>next.config.ts</files>
  <what-built>
  Next 16.2.12 pin plus enforce-mode HSTS and allowlist CSP in `next.config.ts` covering HubSpot, GA, YouTube, and the blog iframe host.
  </what-built>
  <action>
  Pause for human preview smoke after Tasks 1–2. Do not change code unless the human reports CSP violation hosts — then expand the allowlist narrowly in `next.config.ts` and re-run smoke. Do not switch to Report-Only or nonce CSP.
  </action>
  <how-to-verify>
  1. Start preview (`npm run build && npm run start`, or the Vercel preview deploy for this branch).
  2. Open `/`, `/contact-us`, `/solutions`, and `/blog` with browser DevTools Console + Network.
  3. Confirm no CSP violations blocking: HubSpot form script/submit on contact, gtag on pages with GA, YouTube facade iframe on solutions, blog iframe on `/blog`, bfcache recovery inline script (no unexpected inline-script blocks).
  4. Confirm response headers include `Strict-Transport-Security` and `Content-Security-Policy` (not Report-Only).
  5. If an unexpected required host appears, expand the allowlist narrowly and re-smoke — do not switch to Report-Only or nonce CSP.
  </how-to-verify>
  <verify>
    <automated>MISSING — human browser CSP smoke; confirm headers via DevTools Network on /</automated>
  </verify>
  <done>Human approves that home, contact-us, solutions, and blog load without required-third-party CSP breakage; HSTS and enforce CSP headers visible.</done>
  <resume-signal>Type "approved" if pages load cleanly, or list CSP violation hosts/URLs to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← HTML response headers | Transport and content policies enforced by the browser |
| npm registry → lockfile | Framework patch upgrades change server/build attack surface |
| Third-party CDNs (HubSpot, GA, YouTube, blog host) → page | Script/frame/connect loads must be explicitly allowlisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Denial of Service / Elevation | `next` runtime | mitigate | Pin/upgrade to 16.2.12 clearing July 2026 high advisories (SEC-03) |
| T-10-05 | Tampering | HTML responses | mitigate | Enforce allowlist CSP in `next.config.ts` headers (SEC-04) |
| T-10-06 | Tampering | HTTP downgrade | mitigate | Explicit HSTS with includeSubDomains; preload (SEC-04) |
| T-10-07 | Spoofing | Clickjacking | accept | Already mitigated by X-Frame-Options DENY; reinforce with frame-ancestors 'none' in CSP |
| T-10-08 | Tampering | Third-party embeds | mitigate | Explicit origin allowlist only for required HubSpot/GA/YouTube/blog hosts; HTTPS hosts |
| T-10-SC | Tampering | npm installs | mitigate | Upgrade only existing official `next` + `eslint-config-next` (Package Legitimacy Audit Approved); no new packages; human CSP smoke before merge |
</threat_model>

<verification>
- `npm ls next` shows 16.2.12
- Production response headers include HSTS and Content-Security-Policy
- Human smoke: home, contact-us, solutions, blog without required-third-party CSP breakage
</verification>

<success_criteria>
- SEC-03: patched Next pin + green build
- SEC-04: HSTS + enforce CSP present; marketing third parties allowed; smoke approved
</success_criteria>

<output>
Create `.planning/phases/10-security-hygiene/10-02-SUMMARY.md` when done
</output>
