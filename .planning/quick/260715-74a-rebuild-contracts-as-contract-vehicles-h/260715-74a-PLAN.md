---
id: 260715-74a
slug: rebuild-contracts-as-contract-vehicles-h
status: active
created: 2026-07-15
---

# Quick Plan: Contract Vehicles Hub

## Goal
Rebuild `/contracts` as a themed Contract Vehicles hub and add it to top nav. Hub only (no partners gallery). Vehicles: NASA SEWP VI, NYS OGS/PBITS, plus GSA line cards (Commvault, Synnex, HP Coming Soon). Exclude STARS III.

## Tasks

### Task 1 — Content model + assets
- Expand `contracts.ts` with `contractsPageContent` (hero, primary vehicles, GSA line cards, CTA).
- Add `contractsPageSchema` to `schema.ts`; keep `contractsContent` `ContractItem[]` for schema/compat.
- Wire `load-content.ts` + `validate-content.mjs`.
- Local assets under `public/images/contracts/` and `public/documents/contracts/`.

### Task 2 — Page UI + nav
- Replace stub `app/(marketing)/contracts/page.tsx` with SolutionsHero + vehicle sections matching marketing theme.
- CSS in `marketing-secondary.css`.
- Add “Contract Vehicles” nav item in `site.ts` → `/contracts`.
- Update journey tests; ensure page still links to `/services` and `/contact-us`.

## Done when
- `/contracts` shows redesigned hub with the five vehicles (no STARS III, no partners gallery).
- Top nav includes Contract Vehicles.
- Content validates; relevant tests pass.
